"use strict";
/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["__cmc_webpackJsonp__"] = self["__cmc_webpackJsonp__"] || []).push([["vendors-node_modules_splunk_search-job_SearchJob_js-node_modules_splunk_time-range-utils_time-4dc0e5"],{

/***/ "./node_modules/@splunk/search-job/SearchJob.js":
/*!******************************************************!*\
  !*** ./node_modules/@splunk/search-job/SearchJob.js ***!
  \******************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports[\"default\"] = void 0;\n\nvar _get = _interopRequireDefault(__webpack_require__(/*! lodash/get */ \"./node_modules/lodash/get.js\"));\n\nvar _has = _interopRequireDefault(__webpack_require__(/*! lodash/has */ \"./node_modules/lodash/has.js\"));\n\nvar _isNull = _interopRequireDefault(__webpack_require__(/*! lodash/isNull */ \"./node_modules/lodash/isNull.js\"));\n\nvar _memoize = _interopRequireDefault(__webpack_require__(/*! lodash/memoize */ \"./node_modules/lodash/memoize.js\"));\n\nvar _rxjs = __webpack_require__(/*! rxjs */ \"./node_modules/rxjs/Rx.js\");\n\nvar _search = __webpack_require__(/*! @splunk/splunk-utils/search */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/search.js\");\n\nvar _savedSearch = __webpack_require__(/*! @splunk/splunk-utils/savedSearch */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/savedSearch.js\");\n\nvar _progressTracker = __webpack_require__(/*! ./progressTracker */ \"./node_modules/@splunk/search-job/progressTracker.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\nfunction ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }\n\nfunction _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }\n\nfunction _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }\n\nfunction _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nfunction _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }\n\nfunction _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }\n\nfunction _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }\n\n/**\n * A class that simplifies creating and accessing Splunk search jobs. All of the instance\n * methods return [Observables](http://reactivex.io/rxjs/manual/overview.html#introduction).\n *\n * Although `SearchJob`'s instance methods return Observables, `SearchJob` itself is not an\n * Observable. Calling `SearchJob.create` or `SearchJob.fromSavedSearch` makes the appropriate\n * requests to the server immediately, rather than waiting for a subscriber to one of its instance methods.\n * @file\n */\nfunction returnTrue() {\n  return true;\n}\n\nfunction isDone(searchState) {\n  return (0, _get[\"default\"])(searchState, ['content', 'isDone'], false) || (0, _get[\"default\"])(searchState, ['content', 'isFailed'], false) || (0, _get[\"default\"])(searchState, ['content', 'isFinalized'], false);\n}\n\nvar maxSafeInterval = Math.pow(2, 31) - 1;\n\nvar SearchJob = /*#__PURE__*/function () {\n  _createClass(SearchJob, null, [{\n    key: \"create\",\n\n    /**\n     * Create a new `SearchJob` instance. This instance can represent a new *or* an existing search job\n     * on the server depending on the cache option and the current search jobs on the server.\n     *\n     * Some things to keep in mind when working with search jobs and the cache:\n     * * Never call `cancel` or set `cancelOnUnload` when `cache` is `true`. This might cancel a search job used elsewhere.\n     * * When `cache` is `false`, call `cancel` on the search job when done with it to free up resources.\n     * * When `cache` is `true` and the search job is no longer needed locally, call `stopKeepAlive` to free up resources.\n     * * If there aren't any follow-up actions on the search job, consider setting `keepAlive` to `false`.\n     *\n     * Examples\n     * ```js\n     * // Create a simple search\n     * const mySearchJob = SearchJob.create({\n     *     search: 'index=_internal | head 10',\n     *     earliest_time: '-60m@m',\n     *     latest_time: 'now',\n     * });\n     *\n     * // Create a search in a specific context\n     * const mySearchJob = SearchJob.create({\n     *     search: 'index=_internal | head 10',\n     *     earliest_time: '-60m@m',\n     *     latest_time: 'now',\n     * }, {\n     *     app: 'awesome_app',\n     *     owner: 'admin',\n     * });\n     * ```\n     *\n     * @param {Object} searchParams - Search parameters. See the [Splunk documentation on the\n     * `search/jobs` REST endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs)\n     * for available parameters.\n     * @param {String} searchParams.search - The search string. This is the only required search param.\n     * @param {Object} [dispatchOptions] - Dispatch options.\n     * @param {String} [dispatchOptions.app] - App namespace to use.\n     * @param {String} [dispatchOptions.owner] - User namespace to use.\n     * @param {String} [dispatchOptions.sharing] - Must be one of [ 'app' | 'global' | 'system' ].\n     * @param {Boolean} [dispatchOptions.keepAlive=false] - Keep the search alive after it completes by polling\n     * the touch endpoint. Call `stopKeepAlive` (or `cancel`) when the search job is no longer needed.\n     * @param {Boolean} [dispatchOptions.cache=false] - If true, will try and reuse an existing\n     * search job if it has the same request params.\n     * @param {Boolean} [options.cancelOnUnload=!options.cache] - Cancel the search job when the page\n     * unloads. Defaults to true if cache is false. Do not use with cached search jobs.\n     * @param {Number} [dispatchOptions.cacheLimit=600] - The maximum age (in seconds) of a search\n     * job to use if using a cached job.\n     * @returns {SearchJob} A new SearchJob instance.\n     * @public\n     */\n    value: function create(searchParams) {\n      var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n\n      var app = _ref.app,\n          owner = _ref.owner,\n          sharing = _ref.sharing,\n          _ref$cache = _ref.cache,\n          cache = _ref$cache === void 0 ? false : _ref$cache,\n          _ref$cacheLimit = _ref.cacheLimit,\n          cacheLimit = _ref$cacheLimit === void 0 ? 600 : _ref$cacheLimit,\n          _ref$cancelOnUnload = _ref.cancelOnUnload,\n          cancelOnUnload = _ref$cancelOnUnload === void 0 ? !cache : _ref$cancelOnUnload,\n          constructorOptions = _objectWithoutProperties(_ref, [\"app\", \"owner\", \"sharing\", \"cache\", \"cacheLimit\", \"cancelOnUnload\"]);\n\n      var search = (0, _search.addLeadingSearchCommand)(searchParams.search);\n\n      var qualifiedSearchParams = _objectSpread(_objectSpread({}, searchParams), {}, {\n        search: search\n      });\n\n      var searchJobSid = cache ? (0, _search.getCachedSearch)(qualifiedSearchParams, {\n        cacheLimit: cacheLimit\n      }, SearchJob.splunkConfig, SearchJob.baseFetchInit).then(function (cachedSearch) {\n        var sid = (0, _get[\"default\"])(cachedSearch, ['content', 'sid'], null);\n\n        if ((0, _isNull[\"default\"])(sid)) {\n          return (0, _search.createSearchJob)(_objectSpread({\n            'webframework.cache.hash': (0, _search.createJava5HashForString)(search)\n          }, qualifiedSearchParams), {\n            app: app,\n            owner: owner,\n            sharing: sharing\n          }, SearchJob.splunkConfig, SearchJob.baseFetchInit);\n        }\n\n        return {\n          sid: sid\n        };\n      }) : (0, _search.createSearchJob)(qualifiedSearchParams, {\n        app: app,\n        owner: owner,\n        sharing: sharing\n      }, SearchJob.splunkConfig, SearchJob.baseFetchInit);\n      return new SearchJob(searchJobSid, _objectSpread({\n        cancelOnUnload: cancelOnUnload\n      }, constructorOptions));\n    }\n    /**\n     * Creates a search job from a saved search.\n     *\n     * Example:\n     * ```js\n     * const mySearchJob = SearchJob.fromSavedSearch({\n     *     name: 'My Saved Search',\n     *     app: 'search',\n     *     owner: 'me',\n     * }, { cache: 'scheduled' });\n     * ```\n     *\n     * @param {Object} savedSearch - Identify the target saved search by its name, app, and owner.\n     * Name is always required. App and owner might be required depending on the permissions of the\n     * target saved search.\n     * @param {String} savedSearch.name - The name of the target saved search.\n     * @param {String} [savedSearch.app] - The app of the target saved search.\n     * @param {String} [savedSearch.owner] - The owner of the target saved search.\n     * @param {Object} [options={}]\n     * @param {Object} [options.dispatchParams={}] - Parameters to use when dispatching a new the\n     * search job. Ignored if a cached search job is used. See [Splunk REST API docs for the\n     * `saved/searches/${name}/dispatch` endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#saved.2Fsearches.2F.7Bname.7D.2Fdispatch)\n     * for available parameters.\n     * @param {Boolean} [options.keepAlive=false] - Keep the search alive after it completes by polling\n     * the touch endpoint. Call `stopKeepAlive` (or `cancel`) when the search job is no longer needed.\n     * @param {Boolean} [options.cancelOnUnload=false] - Cancel the search job when the page\n     * unloads. This will impact others if the search job is shared.\n     * @param {String} [options.cache='any'] - Use a search job that already exists. Accepted values:\n     * * none - Dispatch a new job\n     * * scheduled - Use the most recent scheduled search job.\n     * * any - Use the most recent search job.\n     * @param {Number} [options.cacheLimit=Infinity] - The maximum age (in seconds) of a search\n     * job to use if using a cached job.\n     * @returns {SearchJob} A new SearchJob instance.\n     * @public\n     */\n\n  }, {\n    key: \"fromSavedSearch\",\n    value: function fromSavedSearch(savedSearch) {\n      var _ref2 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n\n      var _ref2$cache = _ref2.cache,\n          cache = _ref2$cache === void 0 ? 'any' : _ref2$cache,\n          _ref2$cacheLimit = _ref2.cacheLimit,\n          cacheLimit = _ref2$cacheLimit === void 0 ? Infinity : _ref2$cacheLimit,\n          _ref2$dispatchParams = _ref2.dispatchParams,\n          dispatchParams = _ref2$dispatchParams === void 0 ? {} : _ref2$dispatchParams,\n          constructorOptions = _objectWithoutProperties(_ref2, [\"cache\", \"cacheLimit\", \"dispatchParams\"]);\n\n      if (cache !== 'any' && cache !== 'none' && cache !== 'scheduled') {\n        throw new Error(\"Invalid value for cache: '\".concat(cache, \"'. cache must be 'none', 'any', or 'scheduled'\"));\n      }\n\n      var searchJobSid;\n\n      if (cache === 'none') {\n        searchJobSid = (0, _savedSearch.dispatch)(savedSearch, dispatchParams, SearchJob.splunkConfig, SearchJob.baseFetchInit);\n      } else {\n        var params = cache === 'scheduled' ? {\n          search: 'isScheduled=true'\n        } : {};\n        searchJobSid = (0, _savedSearch.getLatest)(savedSearch, params, SearchJob.splunkConfig, SearchJob.baseFetchInit).then(function (latestSearch) {\n          var sid = (0, _get[\"default\"])(latestSearch, ['name'], null);\n\n          if ((0, _isNull[\"default\"])(sid)) {\n            return (0, _savedSearch.dispatch)(savedSearch, dispatchParams, SearchJob.splunkConfig, SearchJob.baseFetchInit);\n          }\n\n          if (cacheLimit > 0) {\n            var threshold = Date.now() / 1000 - cacheLimit;\n\n            if (threshold > latestSearch.content.start) {\n              return (0, _savedSearch.dispatch)(savedSearch, dispatchParams, SearchJob.splunkConfig, SearchJob.baseFetchInit);\n            }\n          }\n\n          return {\n            sid: sid\n          };\n        });\n      }\n\n      return new SearchJob(searchJobSid, constructorOptions);\n    }\n    /**\n     * Creates a SearchJob instance from a search job that already exists on the server.\n     *\n     * Example:\n     * ```js\n     * const mySearchJob = SearchJob.fromSid('sid-of-search-job-that-already-exists');\n     * ```\n     *\n     * @param {String} sid - The search job sid.\n     * @param {Object} [options]\n     * @param {Boolean} [options.keepAlive=false] - Keep the search alive after it completes by polling\n     * the touch endpoint. Call `stopKeepAlive` (or `cancel`) when the search job is no longer needed.\n     * @returns {SearchJob} A new SearchJob instance.\n     * @public\n     */\n\n  }, {\n    key: \"fromSid\",\n    value: function fromSid(sid) {\n      var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      return new SearchJob(_rxjs.Observable.of({\n        sid: sid\n      }), options);\n    }\n    /**\n     * Sets the splunk config values used when creating requests to the server. Defaults to the\n     * values provided by `@splunk/splunk-utils/config`.\n     *\n     * This can be used to run searches outside of a Splunkweb environment.\n     *\n     * This is an advanced API and not relevant for typical usage.\n     *\n     * **Warning:** The configuration will be set for all requests made by all `SearchJob` instances. It\n     * is not possible at this time to set the splunk config on an individual `SearchJob` instance due to\n     * batching of requests.\n     *\n     * @param {Object} [splunkConfig]\n     * @public\n     */\n\n  }, {\n    key: \"setSplunkConfig\",\n    value: function setSplunkConfig(splunkConfig) {\n      SearchJob.splunkConfig = splunkConfig;\n      (0, _progressTracker.setSplunkConfig)(splunkConfig);\n    }\n    /**\n     * Sets the base fetch init used when creating requests to the server. Defaults to the\n     * defaultFetchInit provided by `@splunk/splunk-utils/fetch`.\n     *\n     * This can be used to set up authentication when running searches outside of a Splunkweb\n     * environment.\n     *\n     * This is an advanced API and not relevant for typical usage.\n     *\n     * **Warning:** The base fetch init will be set for all requests made by all `SearchJob` instances. It\n     * is not possible at this time to set the base fetch init on an individual `SearchJob` instance due to\n     * batching of requests.\n     *\n     * @param {Object} [baseFetchInit]\n     * @public\n     */\n\n  }, {\n    key: \"setBaseFetchInit\",\n    value: function setBaseFetchInit(baseFetchInit) {\n      SearchJob.baseFetchInit = baseFetchInit;\n      (0, _progressTracker.setBaseFetchInit)(baseFetchInit);\n    }\n  }, {\n    key: \"defaultPreviewFetchPredicate\",\n\n    /**\n     * The default fetch predicate for `SearchJob#getResultsPreview`.\n     *\n     * @param {Object} searchState\n     * @returns {Boolean} - True if it is a real time search or there are preview results.\n     * @private\n     */\n    value: function defaultPreviewFetchPredicate(searchState) {\n      if ((0, _get[\"default\"])(searchState, ['content', 'isRealTimeSearch'], false)) {\n        return true;\n      }\n\n      return (0, _get[\"default\"])(searchState, ['content', 'resultPreviewCount'], 0) > 0;\n    }\n    /**\n     * An internal utility method that makes a request to `search/jobs/job/${endpoint}`.\n     *\n     * @param {String} sid - The sid of the search job.\n     * @param {String} endpoint - The endpoint to poll.\n     * @param {Object} [requestParams] - Specific to the endpoint.\n     * @returns {Observable}\n     * @private\n     */\n\n  }, {\n    key: \"getData\",\n    value: function getData(sid, endpoint, requestParams) {\n      return _rxjs.Observable.fromPromise((0, _search.getData)(sid, endpoint, requestParams, SearchJob.splunkConfig, SearchJob.baseFetchInit));\n    }\n    /**\n     * Constructor. Generally, use the static create methods instead.\n     *\n     * @param {Promise|Observable} searchJobSid - Resolves with an object containing the sid of a\n     * search job on the server.\n     * @param {Object} [options={}]\n     * @param {Boolean} [options.cancelOnUnload=false] - Cancel the search job when the page unloads.\n     * Do not use with cached search jobs.\n     * @param {Boolean} [options.keepAlive=false] - Keep the search alive after it completes by polling\n     * the touch endpoint. Call `stopKeepAlive` or `cancel` when the search job is no longer needed.\n     * @returns {SearchJob} this\n     * @private\n     */\n\n  }]);\n\n  function SearchJob(searchJobSid) {\n    var _this = this;\n\n    var _ref3 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},\n        _ref3$cancelOnUnload = _ref3.cancelOnUnload,\n        cancelOnUnload = _ref3$cancelOnUnload === void 0 ? false : _ref3$cancelOnUnload,\n        _ref3$keepAlive = _ref3.keepAlive,\n        keepAlive = _ref3$keepAlive === void 0 ? false : _ref3$keepAlive;\n\n    _classCallCheck(this, SearchJob);\n\n    _defineProperty(this, \"handleUnload\", function () {\n      _this.cancel();\n    });\n\n    this.searchJobSid = _rxjs.Observable.from(searchJobSid);\n    ['getSid', 'getProgress', 'whenDone'].forEach(function (method) {\n      _this[method] = (0, _memoize[\"default\"])(_this[method]);\n    });\n    this.ttl = null;\n\n    if (cancelOnUnload && typeof window !== 'undefined' && (0, _has[\"default\"])(window, 'addEventListener')) {\n      window.addEventListener('beforeunload', this.handleUnload);\n    }\n\n    if (keepAlive) {\n      this.startKeepAlive();\n    } else {\n      this.keepAliveSubscription = null;\n    }\n  }\n  /**\n   * Returns an Observable that emits the sid once the search is created on the server.\n   *\n   * Example:\n   * ```js\n   * const sidSubscription = mySearchJob.getSid().subscribe(sid => {\n   *     // Do something with the sid.\n   * });\n   * ```\n   *\n   * @returns {Observable}\n   * @public\n   */\n\n\n  _createClass(SearchJob, [{\n    key: \"getSid\",\n    value: function getSid() {\n      return this.searchJobSid.map(function (_ref4) {\n        var sid = _ref4.sid;\n        return sid;\n      });\n    }\n    /**\n     * Returns an Observable that emits search progress updates.\n     *\n     * Simple Example:\n     * ```js\n     * const progressSubscription = mySearchJob.getProgress().subscribe(searchState => {\n     *     // Do something with the searchState.\n     * });\n     *\n     * // If the search is not done, but is not longer needed, unsubscribe to release resources.\n     * progressSubscription.unsubscribe();\n     * ```\n     *\n     * Example handling errors and completion:\n     * ```js\n     * const progressSubscription = mySearchJob.getProgress().subscribe({\n     *     next: searchState => {\n     *         // Do something with the search state.\n     *     },\n     *     error: err => {\n     *         // The search failed. Do something with the err.\n     *     },\n     *     complete: () => {\n     *         // The search has completed successfully.\n     *     },\n     * });\n     * ```\n     *\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"getProgress\",\n    value: function getProgress() {\n      return this.getSid().concatMap(_progressTracker.track).publishReplay(1).refCount();\n    }\n    /**\n     * Returns an Observable that emits when the search is done.\n     *\n     * Example:\n     * ```js\n     * const doneSubscription = mySearchJob.whenDone().subscribe(searchState => {\n     *     // Do something with the searchState.\n     * });\n     *\n     * // If the search is not done, but is not longer needed, unsubscribe to release resources.\n     * doneSubscription.unsubscribe();\n     * ```\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"whenDone\",\n    value: function whenDone() {\n      return this.getProgress().last().publishReplay(1).refCount();\n    }\n    /**\n     * Returns an Observable that emits results when the search is done.\n     *\n     * Example:\n     * ```js\n     * const resultsSubscription = mySearchJob.getResults().subscribe(results => {\n     *     // Do something with the results.\n     * });\n     *\n     * // If the search is not done but is not longer needed, unsubscribe to release resources.\n     * resultsSubscription.unsubscribe();\n     * ```\n     * @param requestParams {Object} - [See documentation for available parameters.\n     * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fresults)\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"getResults\",\n    value: function getResults() {\n      var requestParams = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};\n      return this.whenDone().concatMap(function (searchState) {\n        return SearchJob.getData(searchState.content.sid, 'results', requestParams);\n      }).publishReplay(1).refCount();\n    }\n    /**\n     * An internal utility method that polls search/jobs/job/${endpoint} on progress updates emitted by\n     * getProgress.\n     *\n     * @param {String} endpoint - The endpoint to poll.\n     * @param {Object} [requestParams] - Specific to the endpoint.\n     * @param {Object} [dispatchOptions]\n     * @param {Function} [dispatchOptions.fetchPredicate] - Return false to skip fetching for a\n     * particular beat of progress. The function is passed the search state.\n     * @returns {Observable}\n     * @private\n     */\n\n  }, {\n    key: \"pollData\",\n    value: function pollData(endpoint) {\n      var requestParams = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      var dispatchOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n      var fetchPredicate = dispatchOptions.fetchPredicate || returnTrue; // About this implementation:\n      // `exhaustMap` is used to avoid race conditions. This ignores a progress update if\n      // data is still being fetched for the previous progress update. Ignoring a random progress\n      // update to let the result data catch up is fine, as long as it is not the final progress\n      // update. To avoid skipping the final update, we filter it out of the initial Observable\n      // and concat it after exhaustMap. This ensures that the final progress update executes\n      // once and only once.\n\n      return this.getProgress().filter(fetchPredicate).filter(function (searchState) {\n        return !isDone(searchState);\n      }).exhaustMap(function (searchState) {\n        return SearchJob.getData(searchState.content.sid, endpoint, requestParams);\n      }).concat(this.whenDone().filter(fetchPredicate).concatMap(function (searchState) {\n        return SearchJob.getData(searchState.content.sid, endpoint, requestParams);\n      })).publishReplay(1).refCount();\n    }\n    /**\n     * Returns an Observable that emits result previews.\n     *\n     * Example:\n     * ```js\n     * const resultsPreviewSubscription = mySearchJob.getResultsPreview().subscribe(results => {\n     *     // Do something with the results.\n     * });\n     *\n     * // If the search is not done but is not longer needed, unsubscribe to release resources.\n     * resultsPreviewSubscription.unsubscribe();\n     * ```\n     *\n     * @param {Object} requestParams - [See documentation for available parameters.\n     * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fresults_preview)\n     * @param {Object} [dispatchOptions]\n     * @param {Function} [dispatchOptions.fetchPredicate] - Return false to skip fetching for a\n     * particular beat of progress. The function is passed the search state.\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"getResultsPreview\",\n    value: function getResultsPreview() {\n      var requestParams = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};\n      var dispatchOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      return this.pollData('results_preview', requestParams, _objectSpread({\n        fetchPredicate: SearchJob.defaultPreviewFetchPredicate\n      }, dispatchOptions));\n    }\n    /**\n     * Returns an Observable that emits events.\n     *\n     * Example:\n     * ```js\n     * const eventsSubscription = mySearchJob.getEvents().subscribe(events => {\n     *     // Do something with the events.\n     * });\n     *\n     * // If the search is not done but is not longer needed, unsubscribe to release resources.\n     * eventsSubscription.unsubscribe();\n     * ```\n     *\n     * @param {Object} requestParams - [See documentation for available parameters.\n     * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fevents)\n     * @param {Object} [dispatchOptions]\n     * @param {Function} [dispatchOptions.fetchPredicate] - Return false to skip fetching for a\n     * particular beat of progress. The function is passed the search state.\n     * @public\n     */\n\n  }, {\n    key: \"getEvents\",\n    value: function getEvents() {\n      var requestParams = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};\n      var dispatchOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      return this.pollData('events', requestParams, dispatchOptions);\n    }\n    /**\n     * Returns an Observable that emits the search summary.\n     *\n     * Example:\n     * ```js\n     * const summarySubscription = mySearchJob.getSummary().subscribe(summary => {\n     *     // Do something with the summary.\n     * });\n     *\n     * // If the search is not done but is not longer needed, unsubscribe to release resources.\n     * summarySubscription.unsubscribe();\n     * ```\n     *\n     * @param {Object} requestParams - [See documentation for available parameters.\n     * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fsummary)\n     * @param {Object} [dispatchOptions]\n     * @param {Function} [dispatchOptions.fetchPredicate] - Return false to skip fetching for a\n     * particular beat of progress. The function is passed the search state.\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"getSummary\",\n    value: function getSummary() {\n      var requestParams = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};\n      var dispatchOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      return this.pollData('summary', requestParams, dispatchOptions);\n    }\n    /**\n     * Returns an Observable that emits the search timeline.\n     *\n     * Example:\n     * ```js\n     * const timelineSubscription = mySearchJob.getTimeline().subscribe(timeline => {\n     *     // Do something with the timeline.\n     * });\n     *\n     * // If the search is not done but is not longer needed, unsubscribe to release resources.\n     * timelineSubscription.unsubscribe();\n     * ```\n     *\n     * @param {Object} requestParams - [See documentation for available parameters.\n     * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Ftimeline)\n     * @param {Object} [dispatchOptions]\n     * @param {Function} [dispatchOptions.fetchPredicate] - Return false to skip fetching for a\n     * particular beat of progress. The function is passed the search state.\n     * @returns {Observable}\n     * @public\n     */\n\n  }, {\n    key: \"getTimeline\",\n    value: function getTimeline() {\n      var requestParams = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};\n      var dispatchOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      return this.pollData('timeline', requestParams, dispatchOptions);\n    }\n    /**\n     * Helper method for applying actions to a search.\n     *\n     * @param {String} action - The action to take.\n     * @param {Object} [params] - Any additional params supported by the particular action.\n     * @returns {Observable} A hot Observable.\n     * @private\n     */\n\n  }, {\n    key: \"control\",\n    value: function control(action) {\n      var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n      var obs = this.getSid().concatMap(function (sid) {\n        return _rxjs.Observable.fromPromise((0, _search.postAction)(sid, _objectSpread({\n          action: action\n        }, params), SearchJob.splunkConfig, SearchJob.baseFetchInit));\n      }).publishReplay(1);\n      obs.connect();\n      return obs;\n    }\n    /**\n     * Pauses the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.pause();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.pause().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"pause\",\n    value: function pause() {\n      return this.control('pause');\n    }\n    /**\n     * Unpauses the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.unpause();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.unpause().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"unpause\",\n    value: function unpause() {\n      return this.control('unpause');\n    }\n    /**\n     * Finalizes the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.finalize();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.finalize().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"finalize\",\n    value: function finalize() {\n      return this.control('finalize');\n    }\n    /**\n     * Cancels the search.\n     *\n     * Warning: Make sure there are no subscribers to the search job before cancelling it.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.cancel();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.cancel().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"cancel\",\n    value: function cancel() {\n      this.stopKeepAlive();\n\n      if (typeof window !== 'undefined' && (0, _has[\"default\"])(window, 'removeEventListener')) {\n        window.removeEventListener('beforeunload', this.handleUnload);\n      }\n\n      return this.control('cancel');\n    }\n    /**\n     * Touches the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.touch();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.touch().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"touch\",\n    value: function touch() {\n      return this.control('touch');\n    }\n    /**\n     * Sets the TTL (time to live) of the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.setTTL(100);\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.setTTL(100).subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @param {Number} nextTTL\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"setTTL\",\n    value: function setTTL(nextTTL) {\n      this.ttl = nextTTL;\n\n      if (this.keepAliveSubscription) {\n        this.startKeepAlive();\n      }\n\n      return this.control('setttl', {\n        ttl: nextTTL\n      });\n    }\n    /**\n     * Sets the priority of the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.setPriority(1);\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.setPriority(1).subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @param {Number} nextPriority\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"setPriority\",\n    value: function setPriority(nextPriority) {\n      return this.control('setpriority', {\n        priority: nextPriority\n      });\n    }\n    /**\n     * Enables previews of the search results.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.enablePreview();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.enablePreview().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"enablePreview\",\n    value: function enablePreview() {\n      return this.control('enablepreview');\n    }\n    /**\n     * Disables previews of the search results.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.disablePreview();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.disablePreview().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"disablePreview\",\n    value: function disablePreview() {\n      return this.control('disablepreview');\n    }\n    /**\n     * Saves the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.save();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.save().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"save\",\n    value: function save() {\n      return this.control('save');\n    }\n    /**\n     * Unsaves the search.\n     *\n     * Example:\n     * ```js\n     * mySearchJob.unsave();\n     * ```\n     *\n     * Example with server response:\n     * ```js\n     * mySearchJob.unsave().subscribe(response => {\n     *     // Do something with the response.\n     * })\n     * ```\n     *\n     * @returns {Observable} A \"hot\" Observable that emits the server response.\n     * @public\n     */\n\n  }, {\n    key: \"unsave\",\n    value: function unsave() {\n      return this.control('unsave');\n    }\n    /**\n     * Keeps the search alive on the server by touching it periodically once it is complete.\n     * @public\n     */\n\n  }, {\n    key: \"startKeepAlive\",\n    value: function startKeepAlive() {\n      var _this2 = this;\n\n      this.stopKeepAlive();\n      this.keepAliveSubscription = this.whenDone()[\"catch\"](function () {\n        return _rxjs.Observable.empty();\n      }).map(function (searchState) {\n        var ttl = (0, _isNull[\"default\"])(_this2.ttl) ? (0, _get[\"default\"])(searchState, ['content', 'ttl']) : _this2.ttl; // Multiple by 500 to convert ttl to millisecond and divide by 2 to touch the search\n        // job halfway through the ttl.\n\n        return Math.min(ttl * 500, maxSafeInterval);\n      }).concatMap(function (interval) {\n        return _rxjs.Observable.interval(interval);\n      }).exhaustMap(function () {\n        return _this2.touch();\n      })[\"catch\"](function (err, caught) {\n        return caught;\n      }) // Retry if touch failed.\n      .subscribe();\n    }\n    /**\n     * Stops the keep alive process. Call this method to dispose of resources if this search job is no\n     * longer used and has a keep alive process. If this search job is not using the cache, consider\n     * using `cancel` instead.\n     * @public\n     */\n\n  }, {\n    key: \"stopKeepAlive\",\n    value: function stopKeepAlive() {\n      if (this.keepAliveSubscription) {\n        this.keepAliveSubscription.unsubscribe();\n        this.keepAliveSubscription = null;\n      }\n    }\n    /**\n     * Called when the user navigates away from the page.\n     * @private\n     */\n\n  }]);\n\n  return SearchJob;\n}();\n\n_defineProperty(SearchJob, \"splunkConfig\", undefined);\n\n_defineProperty(SearchJob, \"baseFetchInit\", undefined);\n\nvar _default = SearchJob;\nexports[\"default\"] = _default;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/SearchJob.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js":
/*!*************************************************************************************!*\
  !*** ./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js ***!
  \*************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.extractAppName = extractAppName;\nexports.versionLabel = exports.username = exports.splunkdPath = exports.serverTimezoneInfo = exports.rootPath = exports.portNumber = exports.locale = exports.config = exports.buildPushNumber = exports.buildNumber = exports.appBuild = exports.app = exports.isAvailable = exports.CSRFToken = void 0;\n\nvar _get = _interopRequireDefault(__webpack_require__(/*! lodash/get */ \"./node_modules/lodash/get.js\"));\n\nvar _cookie = __webpack_require__(/*! @splunk/ui-utils/cookie */ \"./node_modules/@splunk/ui-utils/cookie.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\n/**\n * A set of constants describing the server environment. If window.$C isn't defined, most of the\n * values on this page are also undefined.\n *\n * The constants may be imported independently, such as:\n * ```\n * import { buildNumber, locale, rootPath } from '@splunk/splunk-utils/config';\n * @file\n */\n\n/* global global */\n// If this is running in Node, the root is the `global` object. If it's running in the browser,\n// root is the `window` object.\nvar root = typeof window === 'undefined' ? __webpack_require__.g : window;\n/**\n * A function to get a $C prop. Returns undefined if $C isn't available.\n * @private\n */\n\nfunction get$CEntry(prop) {\n  return (0, _get[\"default\"])(root, ['$C', prop]);\n}\n/**\n * The CSRF token. This may be `null` if the token isn't found in the cookie.\n *\n * @constant\n *\n * @public\n */\n\n\nvar CSRFToken = (0, _cookie.getEntry)(\"splunkweb_csrf_token_\".concat(get$CEntry('MRSPARKLE_PORT_NUMBER')));\n/**\n * If `window.$C` is undefined or invalid, isAvailable is `false`.\n * @constant\n * @public\n */\n\nexports.CSRFToken = CSRFToken;\nvar isAvailable = !!get$CEntry('SPLUNKD_PATH');\n/**\n * Extracts app name from the URL. To enable testing, this function is exported and pathname\n * can be overridden.\n * @private\n */\n\nexports.isAvailable = isAvailable;\n\nfunction extractAppName() {\n  var pathname = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : (0, _get[\"default\"])(root, ['document', 'location', 'pathname'], '');\n  var pathMatch = pathname.match(/\\w\\w.\\w\\w\\/(app|manager)\\/([^/]+)/);\n  return pathMatch ? pathMatch[2] : undefined;\n}\n/**\n * The name of the current app as shown in the URL (not the display name).\n * @constant\n * @public\n */\n\n\nvar app = extractAppName();\n/**\n * The build number of the current app.\n * @constant\n * @public\n */\n\nexports.app = app;\nvar appBuild = get$CEntry('APP_BUILD');\n/**\n * A hash string of the current build.\n * @constant\n * @type {string}\n * @public\n */\n\nexports.appBuild = appBuild;\nvar buildNumber = get$CEntry('BUILD_NUMBER');\n/**\n * A incremented number configured per instance. Defaults to 0.\n * @constant\n * @public\n */\n\nexports.buildNumber = buildNumber;\nvar buildPushNumber = get$CEntry('BUILD_PUSH_NUMBER');\n/**\n * The full object of window.$C.\n * @constant\n * @public\n */\n\nexports.buildPushNumber = buildPushNumber;\nvar config = (0, _get[\"default\"])(root, '$C');\n/**\n * The current locale, such as \"en-US\".\n * @constant\n * @public\n */\n\nexports.config = config;\nvar locale = get$CEntry('LOCALE');\n/**\n * The port that Splunk Web is using. Defaults to 8000.\n * @constant\n * @public\n */\n\nexports.locale = locale;\nvar portNumber = get$CEntry('MRSPARKLE_PORT_NUMBER');\n/**\n * Splunk may not be running in the root directory, so the locale may not be the first element\n * in the pathname. If it is running in the root directory, ```rootPath``` is an empty\n * string. Otherwise it returns the root. For example, in the page\n * ```\n * http://splunk.domain.com/my-root-dir/en-US/app/search/search\n * ```\n * ```rootPath``` is set to ```'my-root-dir'```.\n * @constant\n * @public\n */\n\nexports.portNumber = portNumber;\nvar rootPath = get$CEntry('MRSPARKLE_ROOT_PATH');\n/**\n * The serialized Splunk Enterprise time zone data. This can be parsed for use in formatting Epoch times\n * on the client.\n * @constant\n * @public\n */\n\nexports.rootPath = rootPath;\nvar serverTimezoneInfo = get$CEntry('SERVER_ZONEINFO');\n/**\n * The Splunk Web proxing to splunkd, such as ```/en-US/splunkd/__raw```.\n * @constant\n * @public\n */\n\nexports.serverTimezoneInfo = serverTimezoneInfo;\nvar splunkdPath = get$CEntry('SPLUNKD_PATH');\n/**\n * The username of the current user.\n * @constant\n * @public\n */\n\nexports.splunkdPath = splunkdPath;\nvar username = get$CEntry('USERNAME');\n/**\n * The version label of splunkd.\n * @constant\n * @public\n */\n\nexports.username = username;\nvar versionLabel = get$CEntry('VERSION_LABEL');\nexports.versionLabel = versionLabel;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/fetch.js":
/*!************************************************************************************!*\
  !*** ./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/fetch.js ***!
  \************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.findErrorMessage = findErrorMessage;\nexports.handleResponse = handleResponse;\nexports.handleError = handleError;\nexports.defaultFetchInit = void 0;\n\nvar _find = _interopRequireDefault(__webpack_require__(/*! lodash/find */ \"./node_modules/lodash/find.js\"));\n\nvar _includes = _interopRequireDefault(__webpack_require__(/*! lodash/includes */ \"./node_modules/lodash/includes.js\"));\n\nvar _config = __webpack_require__(/*! ./config */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\n/**\n * A collection of utils for making [fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch)\n * requests to the [Splunk REST API](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTprolog)\n * and working with the responses.\n * @file\n */\n\n/**\n * A default initialization object for fetch requests. It includes the required credentials and headers.\n * @constant\n * @public\n */\nvar defaultFetchInit = {\n  method: 'GET',\n  credentials: 'include',\n  headers: {\n    'X-Splunk-Form-Key': _config.CSRFToken,\n    'X-Requested-With': 'XMLHttpRequest',\n    'Content-Type': 'application/x-www-form-urlencoded'\n  }\n};\n/**\n * Finds an error message, if available, in a Splunk REST API response.\n * @param {Object} response - The response from the Splunk server.\n * @returns {Object|undefined} The error message object, or undefined if no error is found.\n * @public\n */\n// eslint-disable-next-line consistent-return\n\nexports.defaultFetchInit = defaultFetchInit;\n\nfunction findErrorMessage() {\n  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},\n      messages = _ref.messages;\n\n  if (Array.isArray(messages)) {\n    return (0, _find[\"default\"])(messages, function (_ref2) {\n      var type = _ref2.type;\n      return type === 'ERROR' || type === 'FATAL' || type === 'risky_command';\n    });\n  }\n}\n/**\n * Rejects responses with a status different than the expected. Otherwise, resolves with the JSON\n * response or `null` if the response status code is 204.\n *\n * @param {Number|Number[]} expectedStatus - The status code of a successful response.\n * @returns {Function} A response handler.\n * @public\n */\n\n\nfunction handleResponse(expectedStatus) {\n  return function (res) {\n    if ((0, _includes[\"default\"])([].concat(expectedStatus), res.status)) {\n      return res.status === 204 ? null : res.json();\n    }\n\n    return Promise.reject(res);\n  };\n}\n/**\n * Handles a failed Splunk REST API response by looking for an error message in the response. Returns\n * a rejection for further handling by the end consumer.\n *\n * @param {String} defaultMessage - This is used if a message from the server\n * is not found or if the response from the server couldn't be parsed as JSON.\n * @returns {Function} An error handler.\n * @public\n */\n\n\nfunction handleError(defaultMessage) {\n  return function (res) {\n    var dfd = null;\n\n    try {\n      dfd = res.json();\n    } catch (e) {\n      // If body is not JSON\n      return Promise.reject(new Error(defaultMessage));\n    }\n\n    return dfd.then(function (data) {\n      // If the action failed, most of the time you get an error message.\n      var errorMessage = findErrorMessage(data);\n\n      if (errorMessage) {\n        return Promise.reject(new Error(errorMessage.text));\n      } // Sometimes there is no error message.\n\n\n      return Promise.reject(new Error(defaultMessage));\n    });\n  };\n}\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/fetch.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/savedSearch.js":
/*!******************************************************************************************!*\
  !*** ./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/savedSearch.js ***!
  \******************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.get = get;\nexports.dispatch = dispatch;\nexports.getHistory = getHistory;\nexports.getLatest = getLatest;\n\nvar _querystring = _interopRequireDefault(__webpack_require__(/*! querystring */ \"./node_modules/querystring/index.js\"));\n\nvar _fetch = __webpack_require__(/*! ./fetch */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/fetch.js\");\n\nvar _url = __webpack_require__(/*! ./url */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/url.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\nfunction ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }\n\nfunction _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }\n\nfunction _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }\n\n/**\n * A set of utilities for working with saved searches in the Splunk platform, including getting them, dispatching\n * them, and getting their history.\n * @file\n */\n\n/**\n * Makes a GET request to the `saved/searches/{name}` REST API endpoint.\n *\n * @param {Object} savedSearch - Identify the target saved search by its name (required), app (optional), and owner (optional).\n * @param {String} savedSearch.name - The name of the target saved search.\n * @param {String} [savedSearch.app] - The app of the target saved search.\n * @param {String} [savedSearch.owner] - The owner of the target saved search.\n * @param {Object} [params] - The request parameters. See the [Splunk documentation on the\n * `saved/searches/{name}` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#saved.2Fsearches.2F.7Bname.7D)\n * for available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise with the results from the server.\n * @public\n */\nfunction get(_ref) {\n  var name = _ref.name,\n      app = _ref.app,\n      owner = _ref.owner;\n  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  var url = (0, _url.createRESTURL)(\"saved/searches/\".concat(name), {\n    app: app,\n    owner: owner\n  }, configOptions);\n\n  var qs = _querystring[\"default\"].encode(_objectSpread({\n    output_mode: 'json'\n  }, params));\n\n  return fetch(\"\".concat(url, \"?\").concat(qs), baseFetchInit).then((0, _fetch.handleResponse)(200))[\"catch\"]((0, _fetch.handleError)(\"Failed to get saved search: \".concat(name)));\n}\n/**\n * Makes a POST request to the `saved/searches/{name}/dispatch` REST API endpoint.\n *\n * @param {Object} savedSearch - Identify the target saved search by its name (required), app (optional), and owner (optional).\n * @param {String} savedSearch.name - The name of the target saved search.\n * @param {String} [savedSearch.app] - The app of the target saved search.\n * @param {String} [savedSearch.owner] - The owner of the target saved search.\n * @param {Object} [params] - The request parameters. See the [Splunk documentation on the\n * `saved/searches/{name}/dispatch` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#saved.2Fsearches.2F.7Bname.7D.2Fdispatch)\n * for available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise with the results from the server.\n * @public\n */\n\n\nfunction dispatch(_ref2) {\n  var name = _ref2.name,\n      app = _ref2.app,\n      owner = _ref2.owner;\n  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  var url = (0, _url.createRESTURL)(\"saved/searches/\".concat(name, \"/dispatch\"), {\n    app: app,\n    owner: owner\n  }, configOptions);\n  return fetch(url, _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n    method: 'POST',\n    body: _querystring[\"default\"].encode(_objectSpread(_objectSpread({}, params), {}, {\n      output_mode: 'json'\n    }))\n  })).then((0, _fetch.handleResponse)(201))[\"catch\"]((0, _fetch.handleError)(\"Failed to dispatch saved search: \".concat(name)));\n}\n/**\n * Makes a GET request to the `saved/searches/{name}/history` REST API endpoint.\n *\n * @param {Object} savedSearch - Identify the target saved search by its name (required), app (optional), and owner (optional).\n * @param {String} savedSearch.name - The name of the target saved search.\n * @param {String} [savedSearch.app] - The app of the target saved search.\n * @param {String} [savedSearch.owner] - The owner of the target saved search.\n * @param {Object} [params] - The request parameters. See the [Splunk documentation on the\n * `saved/searches/{name}/history` REST endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#saved.2Fsearches.2F.7Bname.7D.2Fhistory)\n * for available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise with the results from the server.\n * @public\n */\n\n\nfunction getHistory(_ref3) {\n  var name = _ref3.name,\n      app = _ref3.app,\n      owner = _ref3.owner;\n  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  var url = (0, _url.createRESTURL)(\"saved/searches/\".concat(name, \"/history\"), {\n    app: app,\n    owner: owner\n  }, configOptions);\n\n  var qs = _querystring[\"default\"].encode(_objectSpread({\n    output_mode: 'json'\n  }, params));\n\n  return fetch(\"\".concat(url, \"?\").concat(qs), baseFetchInit).then((0, _fetch.handleResponse)(200))[\"catch\"]((0, _fetch.handleError)(\"Failed to get history for saved search: \".concat(name)));\n}\n/**\n * Gets the most recent search job for the specified saved search.\n *\n * @param {Object} savedSearch - Identify the target saved search by its name (required), app (optional), and owner (optional).\n * @param {String} savedSearch.name - The name of the target saved search.\n * @param {String} [savedSearch.app] - The app of the target saved search.\n * @param {String} [savedSearch.owner] - The owner of the target saved search.\n * @param {Object} [params] - The request parameters. See the [Splunk documentation on the\n * `saved/searches/{name}/history` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#saved.2Fsearches.2F.7Bname.7D.2Fhistory)\n * for available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise that resolves with the latest search job, or `undefined` if no search\n * jobs exist for the specified saved search.\n * @public\n */\n\n\nfunction getLatest(savedSearch) {\n  var params = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  return getHistory(savedSearch, _objectSpread(_objectSpread({}, params), {}, {\n    sort_key: 'start',\n    sort_dir: 'desc',\n    count: 1\n  }), configOptions, baseFetchInit).then(function (data) {\n    return data.entry[0];\n  });\n}\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/savedSearch.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/search.js":
/*!*************************************************************************************!*\
  !*** ./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/search.js ***!
  \*************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.gteSplunkVersion = gteSplunkVersion;\nexports.addLeadingSearchCommand = addLeadingSearchCommand;\nexports.stripLeadingSearchCommand = stripLeadingSearchCommand;\nexports.createJava5HashForString = createJava5HashForString;\nexports.getCachedSearch = getCachedSearch;\nexports.createSearchJob = createSearchJob;\nexports.oneShot = oneShot;\nexports.postAction = postAction;\nexports.getData = getData;\nexports.batchGetSearches = batchGetSearches;\nexports.validateSearch = validateSearch;\n\nvar _startsWith = _interopRequireDefault(__webpack_require__(/*! lodash/startsWith */ \"./node_modules/lodash/startsWith.js\"));\n\nvar _omit = _interopRequireDefault(__webpack_require__(/*! lodash/omit */ \"./node_modules/lodash/omit.js\"));\n\nvar _isEqual = _interopRequireDefault(__webpack_require__(/*! lodash/isEqual */ \"./node_modules/lodash/isEqual.js\"));\n\nvar _maxBy = _interopRequireDefault(__webpack_require__(/*! lodash/maxBy */ \"./node_modules/lodash/maxBy.js\"));\n\nvar _querystring = _interopRequireDefault(__webpack_require__(/*! querystring */ \"./node_modules/querystring/index.js\"));\n\nvar _semver = __webpack_require__(/*! semver */ \"./node_modules/semver/index.js\");\n\nvar _fetch = __webpack_require__(/*! ./fetch */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/fetch.js\");\n\nvar _url = __webpack_require__(/*! ./url */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/url.js\");\n\nvar _config = __webpack_require__(/*! ./config */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\nfunction ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }\n\nfunction _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }\n\nfunction _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }\n\n/**\n * Checks if the given Splunk version is greater than or equal to the given Enterprise or Cloud version number\n * If the given `version` is an Enterprise version (eg. 9.0.0), compares to the given `enterpriseVersion`\n * If the given `version` is a Cloud version (eg. 9.0.2209), compares to the given `cloudVersion`\n * @param {String} version - A Splunk version number\n * @param {String} enterpriseVersion - A Splunk Enterprise version number\n * @param {String} cloudVersion - A Splunk Cloud version number\n * @returns {Boolean}\n */\nfunction gteSplunkVersion(version, enterpriseVersion, cloudVersion) {\n  if (version === undefined) {\n    return false;\n  } // Sometimes the version will have a 4th set of digits which the semver\n  // package considers invalid, so we should drop the 4th set of digits\n\n\n  var semverVersion = version.split('.').slice(0, 3).join('.');\n\n  if ((0, _semver.patch)(semverVersion).toString().length === 4) {\n    return (0, _semver.gte)(semverVersion, cloudVersion);\n  }\n\n  return (0, _semver.gte)(semverVersion, enterpriseVersion);\n}\n\nvar propertiesIgnoredByCache = ['webframework.cache.hash', 'app', 'owner', 'provenance', 'refresh', 'refreshType'];\n/**\n * A set of utilities for creating, getting, and manipulating search jobs in the Splunk platform.\n * @file\n */\n\n/**\n * Returns a fully qualified search string by prepending the \"search \"\n * command of unqualified searches.  This method deems strings as unqualified\n * if it doesn't start with a pipe ( | ) or \"search \".\n *\n * @param {String} search - The search string.\n * @returns {String}\n * @public\n */\n\nfunction addLeadingSearchCommand(search) {\n  var trimmedSearch = search.trim();\n\n  if ((0, _startsWith[\"default\"])(trimmedSearch, '|')) {\n    return search;\n  }\n\n  if ((0, _startsWith[\"default\"])(trimmedSearch, 'search ') && trimmedSearch.length > 7) {\n    return search;\n  }\n\n  return \"search \".concat(trimmedSearch);\n}\n/**\n * Returns an unqualified search string by removing the leading \"search \"\n * command. This method does a simple search at the beginning of the\n * search.\n *\n * @param {String} search - The search string.\n * @returns {String}\n * @public\n */\n\n\nfunction stripLeadingSearchCommand(search) {\n  var trimmedSearch = search.trim();\n\n  if ((0, _startsWith[\"default\"])(trimmedSearch, 'search ')) {\n    return trimmedSearch.substring(7);\n  }\n\n  return search;\n}\n/**\n * Hashes a string using the algorithm from the `java.lang.String` hashCode function in Java SE 5\n * and prepends with \"java5\". This is the hash function used by the splunkjs searchmanager to find\n * similar searches that are already active on the server.\n *\n * The returned hash might be negative.\n *\n * @param {String} s - The string to hash.\n * @returns {String} The hash.\n * @public\n */\n\n\nfunction createJava5HashForString(s) {\n  var h = 0;\n  /* eslint-disable no-plusplus, no-bitwise */\n\n  for (var i = 0, n = s.length; i < n; i++) {\n    // (x|0) converts to 32-bit int\n    h = (31 * h | 0) + s.charCodeAt(i) | 0;\n  }\n  /* eslint-enable no-plusplus, no-bitwise */\n\n\n  h = h.toString(16);\n  return \"java5:\".concat(h);\n}\n/**\n * Makes a request to the search/jobs REST API endpoint to fetch any existing searches matching the provided\n * cache config. To qualify, the search must be created with the same request as the search options\n * provided, less the options specifically called out as propertiesIgnoredByCache.\n *\n * @param searchParameters {Object} - The search parameters. See the [Splunk documentation on the\n * `search/jobs` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs)\n * for available parameters.\n * @param {Object} [cacheOptions] - Options to pass to the caching system.\n * @param {Number} [cacheOptions.cacheLimit] - The maximum age in seconds of any artifact for it to qualify as a valid cached search.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} Resolves with search state if a cached job is found, otherwise resolves `null`.\n * @public\n */\n\n\nfunction getCachedSearch(searchParameters) {\n  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},\n      _ref$cacheLimit = _ref.cacheLimit,\n      cacheLimit = _ref$cacheLimit === void 0 ? 0 : _ref$cacheLimit;\n\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  // mimics the hashing traditional in splunkjs\n  var hash = createJava5HashForString(searchParameters.search);\n\n  var qs = _querystring[\"default\"].encode({\n    output_mode: 'json',\n    search: \"request.webframework.cache.hash=\".concat(hash)\n  });\n\n  return fetch(\"\".concat((0, _url.createRESTURL)('search/jobs', {}, configOptions), \"?\").concat(qs), baseFetchInit).then(function (res) {\n    return res.json();\n  }).then(function (body) {\n    var targetParams = (0, _omit[\"default\"])(searchParameters, propertiesIgnoredByCache);\n    var candidateJobs = body.entry.filter(function (entry) {\n      return (0, _isEqual[\"default\"])(targetParams, (0, _omit[\"default\"])(entry.content.request, propertiesIgnoredByCache));\n    });\n\n    if (candidateJobs.length > 0) {\n      var newestJob = (0, _maxBy[\"default\"])(candidateJobs, function (entry) {\n        return new Date(entry.published).valueOf();\n      });\n      var ageThreshold = cacheLimit <= 0 ? 0 : new Date().valueOf() - cacheLimit * 1000;\n\n      if (new Date(newestJob.published).valueOf() > ageThreshold) {\n        return newestJob;\n      }\n    } // No matched searches, no cached\n\n\n    return null;\n  })[\"catch\"]((0, _fetch.handleError)('Failed to get cached jobs.'));\n}\n/**\n * Makes a request to the search/jobs REST API endpoint to create a new search job.\n *\n * @param searchParameters {Object} - The search parameters. See the [Splunk documentation on the\n * `search/jobs` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs)\n * for available parameters.\n * @param [namespaceOptions] {Object}\n * @param {String} [namespaceOptions.app] - App namespace to use.\n * @param {String} [namespaceOptions.owner] - User namespace to use.\n * @param {Sting} [namespaceOptions.sharing] - Must be one of [ '' | 'app' | 'global' | 'system' ].\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} If successful, the promise resolves with an object containing the new search\n * job's sid.\n * @public\n */\n\n\nfunction createSearchJob(searchParameters) {\n  var namespaceOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  return fetch((0, _url.createRESTURL)('search/jobs', namespaceOptions, configOptions), _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n    method: 'POST',\n    body: _querystring[\"default\"].encode(_objectSpread({\n      output_mode: 'json'\n    }, searchParameters))\n  })).then((0, _fetch.handleResponse)(201))[\"catch\"]((0, _fetch.handleError)('Failed to create search job.'));\n}\n/**\n * Creates a one-shot search in which the search results are returned with the initial request to\n * create the search job.\n * @param searchParameters {Object} - The search parameters. See the [Splunk documentation on the\n * `search/jobs` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs)\n * for available parameters.\n * @param [namespaceOptions] {Object}\n * @param {String} [namespaceOptions.app] - App namespace to use.\n * @param {String} [namespaceOptions.owner] - User namespace to use.\n * @param {Sting} [namespaceOptions.sharing] - Must be one of [ '' | 'app' | 'global' | 'system' ].\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Also includes `versionLabel` which is used\n * to determine the search endpoint version. Defaults to the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} If successful, the promise resolves with the search job results.\n * @public\n */\n\n\nfunction oneShot(searchParameters) {\n  var namespaceOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  var splunkVersion = configOptions.versionLabel || _config.versionLabel;\n  var endpointVersion = gteSplunkVersion(splunkVersion, '9.0.2', '9.0.2209') ? '/v2' : '';\n  return fetch((0, _url.createRESTURL)(\"search\".concat(endpointVersion, \"/jobs\"), namespaceOptions, configOptions), _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n    method: 'POST',\n    body: _querystring[\"default\"].encode(_objectSpread(_objectSpread({\n      output_mode: 'json'\n    }, searchParameters), {}, {\n      exec_mode: 'oneshot'\n    }))\n  })).then((0, _fetch.handleResponse)(200))[\"catch\"]((0, _fetch.handleError)('Failed to create one shot search job'));\n}\n/**\n * Makes a POST request to the `search/jobs/${search_id}/control` REST API endpoint to perform an action on a\n * search job.\n *\n * @param sid {String} - The search ID of the search to perform the action on.\n * @param [parameters] {Object} - The request parameters. See the [Splunk documentation on the\n * `search/jobs/{search_id}/control` REST API endpoint](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fcontrol)\n * for available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise with the results from the server.\n * @public\n */\n\n\nfunction postAction(sid) {\n  var parameters = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n  return fetch((0, _url.createRESTURL)(\"search/jobs/\".concat(sid, \"/control\"), {}, configOptions), _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n    method: 'POST',\n    body: _querystring[\"default\"].encode(_objectSpread(_objectSpread({}, parameters), {}, {\n      output_mode: 'json'\n    }))\n  })).then((0, _fetch.handleResponse)(200))[\"catch\"]((0, _fetch.handleError)(\"Action \".concat(parameters.action, \" failed on search job \").concat(sid)));\n}\n/**\n * Makes a GET request to the `search/${endpointVersion}/jobs/${search_id}/${endpoint}` REST API endpoint to get data\n * from the specified search job.\n *\n * @param sid {String} - The search ID of the search job to get data from.\n * @param [endpoint = ''] {String} - The endpoint to use. Must be one of the following:\n *\n * * default (empty string) - The progress or current state of the job. [See documentation for\n * available parameters.](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D)\n * * events - A list of events matched by the job. [See documentation for available parameters.\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fv2.2Fjobs.2F.7Bsearch_id.7D.2Fevents)\n * * results - Get the search results. Only available when the search job is done or\n * finalized. [See documentation for available parameters.\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fv2.2Fjobs.2F.7Bsearch_id.7D.2Fresults)\n * * results_preview - Get a preview of the search results, if enabled. [See documentation for available parameters.\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fv2.2Fjobs.2F.7Bsearch_id.7D.2Fresults_preview)\n * * summary - Get the `getFieldsAndStats` output of the events to date. [See documentation for available parameters.\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Fsummary)\n * * timeline - Get event distribution over time of the untransformed events read to date. [See documentation for available parameters.\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D.2Ftimeline)\n * @param [parameters] {Object} - See the [Splunk documentation on the endpoint\n * used](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs.2F.7Bsearch_id.7D)\n * for the available parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Also includes `versionLabel` which is used\n * to determine the search endpoint version. Defaults to the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n *\n * @returns {Promise} - A promise with the results from the server.\n * @public\n */\n\n\nfunction getData(sid) {\n  var endpoint = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';\n  var parameters = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var configOptions = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};\n  var baseFetchInit = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : _fetch.defaultFetchInit;\n  // Use V2 endpoints only where available\n  // https://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#Semantic_API_versioning\n  var V2_ENDPOINTS = ['results', 'results_preview', 'events'];\n  var splunkVersion = configOptions.versionLabel || _config.versionLabel;\n  var endpointVersion = V2_ENDPOINTS.includes(endpoint) && gteSplunkVersion(splunkVersion, '9.0.2', '9.0.2209') ? '/v2' : '';\n\n  var qs = _querystring[\"default\"].encode(_objectSpread({\n    output_mode: 'json'\n  }, parameters));\n\n  var url = (0, _url.createRESTURL)(\"search\".concat(endpointVersion, \"/jobs/\").concat(sid).concat(endpoint && \"/\".concat(endpoint)), {}, configOptions);\n\n  if (parameters.search && endpointVersion === '/v2') {\n    return fetch(url, _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n      method: 'POST',\n      body: qs\n    })).then((0, _fetch.handleResponse)([200, 204]))[\"catch\"]((0, _fetch.handleError)(\"Failed to get \".concat(endpoint || 'status', \" for job \").concat(sid)));\n  }\n\n  return fetch(\"\".concat(url, \"?\").concat(qs), baseFetchInit).then((0, _fetch.handleResponse)([200, 204]))[\"catch\"]((0, _fetch.handleError)(\"Failed to get \".concat(endpoint || 'status', \" for job \").concat(sid)));\n}\n/**\n * Gets the status of multiple searches in a single request.\n *\n * @param sids {String[]} - An array of search IDs to get.\n * @param [parameters] {Object} - See the [Splunk documentation on the search/jobs REST API endpoint\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fjobs) for\n * supported parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Defaults to\n * the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} - A promise with the results from the server.\n * @public\n */\n\n\nfunction batchGetSearches(sids) {\n  var parameters = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n\n  var qs = _querystring[\"default\"].encode(_objectSpread({\n    output_mode: 'json',\n    id: sids,\n    count: sids.length\n  }, parameters));\n\n  return fetch(\"\".concat((0, _url.createRESTURL)('search/jobs', {}, configOptions), \"?\").concat(qs), baseFetchInit).then((0, _fetch.handleResponse)(200))[\"catch\"]((0, _fetch.handleError)('Failed to get batch status of jobs.'));\n}\n/**\n * Verifies a search string is valid using the `search/v2/parser` endpoint.\n *\n * @param {String} search - The search string to verify.\n * @param [parameters] {Object} - See the [Splunk documentation on the search/v2/parser endpoint\n * ](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTsearch#search.2Fv2.2Fparser) for\n * supported parameters.\n * @param {Object} [configOptions] - Splunk config options passed to url.createRESTURL. Also includes `versionLabel` which is used\n * to determine the search endpoint version. Defaults to the values provided by @splunk/splunk-utils/config.\n * @param {Object} [baseFetchInit] - Options passed to the fetch call. Defaults to the defaultFetchInit\n * in `@splunk/splunk-utils/fetch`.\n * @returns {Promise} A promise that resolves to an object containing a `valid` property set to\n * `true` if the search string is valid, or `false` if otherwise. If the search string is valid, the\n * object also contains a `data` property set to the response data received from the server. If\n * the search string is invalid, the object also contains an `error` property set to the error\n * message received from the server. If the request itself fails, the promise is rejected.\n * @public\n */\n\n\nfunction validateSearch(search) {\n  var parameters = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n  var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n  var baseFetchInit = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : _fetch.defaultFetchInit;\n\n  var qs = _querystring[\"default\"].encode(_objectSpread({\n    output_mode: 'json',\n    parse_only: true,\n    q: addLeadingSearchCommand(search)\n  }, parameters));\n\n  var splunkVersion = configOptions.versionLabel || _config.versionLabel;\n  var endpointVersion = gteSplunkVersion(splunkVersion, '9.0.2', '9.0.2209') ? '/v2' : '';\n  var response;\n\n  if (endpointVersion === '') {\n    // V1 endpoint only supports GET\n    response = fetch(\"\".concat((0, _url.createRESTURL)('search/parser', {}, configOptions), \"?\").concat(qs), baseFetchInit);\n  } else {\n    // V2 endpoint only supports POST\n    response = fetch(\"\".concat((0, _url.createRESTURL)(\"search\".concat(endpointVersion, \"/parser\"), {}, configOptions)), _objectSpread(_objectSpread({}, baseFetchInit), {}, {\n      method: 'POST',\n      body: qs\n    }));\n  }\n\n  return response.then((0, _fetch.handleResponse)(200)).then(function (data) {\n    return {\n      valid: true,\n      data: data\n    };\n  })[\"catch\"](function (res) {\n    return res.json().then(function (data) {\n      var defaultError = 'Failed to validate search string.';\n      var foundError = (0, _fetch.findErrorMessage)(data);\n      var errorMessage = foundError ? foundError.text : defaultError;\n      return res.status === 400 ? Promise.resolve({\n        valid: false,\n        error: errorMessage\n      }) : Promise.reject(new Error(errorMessage));\n    });\n  });\n}\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/search.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/url.js":
/*!**********************************************************************************!*\
  !*** ./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/url.js ***!
  \**********************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nfunction _typeof(obj) { \"@babel/helpers - typeof\"; if (typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj; }; } return _typeof(obj); }\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.withConfig = withConfig;\nexports.insertCacheBuster = exports.createURL = exports.createStaticURL = exports.createRESTURL = exports.createDocsURL = exports.createAppDocsURL = void 0;\n\nvar _querystring = __webpack_require__(/*! querystring */ \"./node_modules/querystring/index.js\");\n\nvar config = _interopRequireWildcard(__webpack_require__(/*! ./config */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/config.js\"));\n\nfunction _getRequireWildcardCache() { if (typeof WeakMap !== \"function\") return null; var cache = new WeakMap(); _getRequireWildcardCache = function _getRequireWildcardCache() { return cache; }; return cache; }\n\nfunction _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } if (obj === null || _typeof(obj) !== \"object\" && typeof obj !== \"function\") { return { \"default\": obj }; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj[\"default\"] = obj; if (cache) { cache.set(obj, newObj); } return newObj; }\n\nfunction ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); if (enumerableOnly) symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; }); keys.push.apply(keys, symbols); } return keys; }\n\nfunction _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; if (i % 2) { ownKeys(Object(source), true).forEach(function (key) { _defineProperty(target, key, source[key]); }); } else if (Object.getOwnPropertyDescriptors) { Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)); } else { ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } } return target; }\n\nfunction _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }\n\n/**\n * A set of functions for generating Splunk Web URLs. They may be imported independently, such as:\n * ```\n * import { createURL, createStaticURL } from '@splunk/splunk-utils/url';\n * ```\n * @file\n */\nfunction withConfig(_ref) {\n  var appBuild = _ref.appBuild,\n      buildNumber = _ref.buildNumber,\n      buildPushNumber = _ref.buildPushNumber,\n      locale = _ref.locale,\n      rootPath = _ref.rootPath,\n      splunkdPath = _ref.splunkdPath;\n\n  /**\n   * Inserts a cache buster into the given path.\n   *\n   * @param path {String} - The path to inject the cache buster into.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`, and\n   * `buildNumber`. Defaults to the values provided by `@splunk/splunk-utils/config`.\n   * @return {String} Returns the path with the cache buster `/@9850fc87a5ee`.\n   * @private\n   */\n  function insertCacheBuster(path) {\n    var configOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n\n    var options = _objectSpread({\n      appBuild: appBuild,\n      buildPushNumber: buildPushNumber,\n      buildNumber: buildNumber\n    }, configOptions);\n\n    var match = path.match(/(^|\\w\\w-\\w\\w\\/)static\\//);\n\n    if (!match) {\n      return path;\n    }\n\n    var insertPosition = match.index + match[0].length - 1;\n    var isApp = path.match(/(^|\\w\\w-\\w\\w|)static\\/app/);\n    var appBuildString = isApp ? \":\".concat(options.appBuild || 0) : '';\n    var buildPushString = options.buildPushNumber ? \".\".concat(options.buildPushNumber) : '';\n    var cacheBusterString = \"/@\".concat(options.buildNumber).concat(buildPushString).concat(appBuildString);\n    var before = path.substr(0, insertPosition);\n    var after = path.substr(insertPosition);\n    return \"\".concat(before).concat(cacheBusterString).concat(after);\n  }\n  /**\n   * Returns a full path for the current appserver location.\n   * This is required for proxy compatiblity, localization, and cache busting.\n   *\n   * For example:\n   * ```js\n   * createURL('app/search/job'); // \"/splunk/en-US/app/search/job\"\n   * createURL('/app/search/job'); // \"/splunk/en-US/app/search/job\"\n   * createURL('app/search/job', {id:3, user:'me' }); // \"/splunk/en-US/app/search/job?id=3&user=me\"\n   * ```\n   *\n   * Static paths are augmented with a cache buster:\n   * ```js\n   * createURL('/static/js/foo.js'); // \"/splunk/en-US/static/@12345/js/foo.js\"\n   * createURL('/static/js/foo.js'); // \"/splunk/en-US/static/@12345.1/js/foo.js\"\n   * createURL('/static/app/search/js/foo.js'); // \"/splunk/en-US/static/@12345.1:0/app/search/js/foo.js\"\n   * ```\n   *\n   * @public\n   * @param path {String}\n   * @param [queryParams] {Object} An object of the query parameters.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`,\n   * `buildNumber`, `rootPath`, and `locale`. Defaults to the values provided by\n   * `@splunk/splunk-utils/config`.\n   * @return {String} Returns a valid URL with correct root path, locale, and cache buster.\n   * @alias createURL\n   */\n\n\n  function createURL(pathInput, queryParams) {\n    var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n\n    var options = _objectSpread({\n      appBuild: appBuild,\n      buildPushNumber: buildPushNumber,\n      buildNumber: buildNumber,\n      rootPath: rootPath,\n      locale: locale\n    }, configOptions);\n\n    var path = pathInput || '/';\n    var query = queryParams ? \"?\".concat((0, _querystring.stringify)(queryParams)) : '';\n\n    if (path.charAt(0) !== '/') {\n      path = \"/\".concat(path);\n    }\n\n    path = \"\".concat(options.rootPath || '', \"/\").concat(options.locale).concat(path).concat(query);\n    return insertCacheBuster(path, options);\n  }\n  /**\n   * Prepends Splunk Web path information necessary to access a static asset. For example:\n   * ```\n   * createURL('fonts/roboto-regular-webfont.woff'); // \"/splunk/en-US/static/@12345/fonts/roboto-regular-webfont.woff\"\n   * createURL('app/search/fonts/roboto-regular-webfont.woff'); // \"/splunk/en-US/static/@12345.1:0/app/search/fonts/roboto-regular-webfont.woff\"\n   * ```\n   * @public\n   * @param {String} path - A path to a static resource.\n   * @param [queryParams] {Object} An object of the query parameters.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`,\n   * `buildNumber`, `rootPath`, and `locale`. Defaults to the values provided by\n   * `@splunk/splunk-utils/config`.\n   * @return {String} Returns a valid URL with correct root path, locale, and cache buster.\n   * @alias createStaticURL\n   */\n\n\n  function createStaticURL(path) {\n    for (var _len = arguments.length, rest = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {\n      rest[_key - 1] = arguments[_key];\n    }\n\n    return createURL.apply(void 0, [\"static/\".concat(path)].concat(rest));\n  }\n  /**\n   * Creates a fully qualified URL for the specified endpoint.\n   * For example:\n   * ```\n   * createRESTURL('server/info'); // \"/en-US/splunkd/__raw/services/server/info\"\n   * createRESTURL('saved/searches', {app: 'search'}); // \"/en-US/splunkd/__raw/servicesNS/-/search/saved/searches\"\n   * ```\n   * @public\n   * @param {String} endpoint - An endpoint to a REST API.\n   * @param {Object} options\n   * @param {String} [options.app] - App namespace to use.\n   * @param {String} [options.owner] - User namespace to use.\n   * @param {Sting} [options.sharing] - Must be one of [ '' | 'app' | 'global' | 'system' ].\n   * @param {Object} [configOptions] - Config options including `splunkdPath`. Defaults to the value\n   *  provided by `@splunk/splunk-utils/config`.\n   * @return {String} The URL of the REST API endpoint.\n   * @alias createRESTURL\n   */\n\n\n  function createRESTURL(endpoint) {\n    var namespaceOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n    var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n\n    // Return the endpoint if it's a full path.\n    if (/^http[s]?:\\/\\//.test(endpoint)) {\n      return endpoint;\n    }\n\n    var basePath = configOptions.splunkdPath || splunkdPath || ''; // If it's an absolute path,\n\n    if (/^\\/.*/.test(endpoint)) {\n      // Append the splunkdPath if the endpoint includes services. Otherwise, return the endpoint.\n      return /^\\/services/.test(endpoint) ? \"\".concat(basePath).concat(endpoint) : endpoint;\n    } // If neither an app nor owner is provided, use /services/ endpoint.\n\n\n    if (!namespaceOptions.app && !namespaceOptions.owner) {\n      return \"\".concat(basePath, \"/services/\").concat(endpoint);\n    } // Build a namespaced path.\n\n\n    var owner;\n\n    if (namespaceOptions.sharing) {\n      owner = 'nobody';\n    } else if (namespaceOptions.owner) {\n      owner = encodeURIComponent(namespaceOptions.owner);\n    } else {\n      owner = '-';\n    }\n\n    var app;\n\n    if (namespaceOptions.sharing === 'system') {\n      app = 'system';\n    } else if (namespaceOptions.app) {\n      app = encodeURIComponent(namespaceOptions.app);\n    } else {\n      app = '-';\n    }\n\n    return \"\".concat(basePath, \"/servicesNS/\").concat(owner, \"/\").concat(app, \"/\").concat(endpoint);\n  }\n  /** Create docs URL private method.\n   * @param {Object} [params]\n   * @private\n   */\n\n\n  function createDocsURLFromParams(params, configOptions) {\n    return createURL('/help', params, configOptions);\n  }\n  /** Create docs URL using location string.\n   * For example:\n   * ```\n   * createDocsURL('learnmore.timerange.picker'); // \"/en-US/help?location=learnmore.timerange.picker\"\n   * ```\n   * @public\n   * @param {String} location - Required.\n   * @return {String} The URL of the Splunk help document.\n   * @alias createDocsURL\n   */\n\n\n  function createDocsURL(location, configOptions) {\n    return createDocsURLFromParams({\n      location: location\n    }, configOptions);\n  }\n  /** Create app docs URL using location string, which needs to add [appName:appVersion] prefix before the location string.\n   * For example:\n   * ```\n   * createAppDocsURL('learnmore.timerange.picker', { appName: 'splunk_app_aws', appVersion: '5.0.3'}); // \"/en-US/help?location=learnmore.timerange.picker\"\n   * ```\n   * @public\n   * @param {String} location - Required.\n   * @param {Object} options - Required.\n   * @param {String} options.appName - App name.\n   * @param {String} options.appVersion - App version.\n   * @return {String} The URL of the Splunk app help document.\n   * @alias createAppDocsURL\n   */\n\n\n  function createAppDocsURL(location, _ref2, configOptions) {\n    var appName = _ref2.appName,\n        appVersion = _ref2.appVersion;\n    return createDocsURLFromParams({\n      location: \"[\".concat(appName, \":\").concat(appVersion, \"]\").concat(location)\n    }, configOptions);\n  }\n\n  return {\n    createAppDocsURL: createAppDocsURL,\n    createDocsURL: createDocsURL,\n    createRESTURL: createRESTURL,\n    createStaticURL: createStaticURL,\n    createURL: createURL,\n    insertCacheBuster: insertCacheBuster\n  };\n}\n\nvar _withConfig = withConfig(config),\n    createAppDocsURL = _withConfig.createAppDocsURL,\n    createDocsURL = _withConfig.createDocsURL,\n    createRESTURL = _withConfig.createRESTURL,\n    createStaticURL = _withConfig.createStaticURL,\n    createURL = _withConfig.createURL,\n    insertCacheBuster = _withConfig.insertCacheBuster;\n\nexports.insertCacheBuster = insertCacheBuster;\nexports.createURL = createURL;\nexports.createStaticURL = createStaticURL;\nexports.createRESTURL = createRESTURL;\nexports.createDocsURL = createDocsURL;\nexports.createAppDocsURL = createAppDocsURL;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/url.js?");

/***/ }),

/***/ "./node_modules/@splunk/search-job/progressTracker.js":
/*!************************************************************!*\
  !*** ./node_modules/@splunk/search-job/progressTracker.js ***!
  \************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.track = exports.setSplunkConfig = exports.setIntervalTest = exports.setBaseFetchInit = exports.resetTest = void 0;\n\nvar _difference = _interopRequireDefault(__webpack_require__(/*! lodash/difference */ \"./node_modules/lodash/difference.js\"));\n\nvar _isArray = _interopRequireDefault(__webpack_require__(/*! lodash/isArray */ \"./node_modules/lodash/isArray.js\"));\n\nvar _isEmpty = _interopRequireDefault(__webpack_require__(/*! lodash/isEmpty */ \"./node_modules/lodash/isEmpty.js\"));\n\nvar _keys = _interopRequireDefault(__webpack_require__(/*! lodash/keys */ \"./node_modules/lodash/keys.js\"));\n\nvar _rxjs = __webpack_require__(/*! rxjs */ \"./node_modules/rxjs/Rx.js\");\n\nvar _search = __webpack_require__(/*! @splunk/splunk-utils/search */ \"./node_modules/@splunk/search-job/node_modules/@splunk/splunk-utils/search.js\");\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { \"default\": obj }; }\n\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nfunction _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }\n\nfunction _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }\n\nfunction _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }\n\n/**\n * A class that polls the server for the status of one or more jobs. This module only exports\n * a singleton of this class, not the class itself.\n * @private\n */\nvar ProgressTracker = /*#__PURE__*/function () {\n  function ProgressTracker() {\n    var _this = this;\n\n    _classCallCheck(this, ProgressTracker);\n\n    _defineProperty(this, \"setSplunkConfig\", function (splunkConfig) {\n      _this.splunkConfig = splunkConfig;\n    });\n\n    _defineProperty(this, \"setBaseFetchInit\", function (baseFetchInit) {\n      _this.baseFetchInit = baseFetchInit;\n    });\n\n    _defineProperty(this, \"setIntervalTest\", function (longInterval) {\n      var shortInterval = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : longInterval;\n\n      if (false) {} else {\n        throw new Error('progressTracker#setIntervalTest is only available in test environment.');\n      }\n    });\n\n    _defineProperty(this, \"resetTest\", function () {\n      if (false) {} else {\n        throw new Error('progressTracker#resetTest is only available in test environment.');\n      }\n    });\n\n    _defineProperty(this, \"track\", function (sid) {\n      // If we already have an observable for the requested search, return it.\n      if (_this.observableCache[sid]) {\n        return _this.observableCache[sid];\n      } // Otherwise, create a new observable for the search, add it to the cache, and return it.\n\n\n      _this.observableCache[sid] = _rxjs.Observable.create(function (observer) {\n        _this.searchSubscribers[sid] = observer;\n\n        _this.queueNextFetch(); // Return the unsubscribe function.\n\n\n        return function () {\n          delete _this.searchSubscribers[sid];\n          delete _this.observableCache[sid];\n\n          if ((0, _isEmpty[\"default\"])(_this.searchSubscribers)) {\n            _this.unqueueNextFetch();\n          }\n        };\n      }) // Use publish replay to make this Observable shareable, with the last update cached.\n      // New subscribers will receive the last update immediately.\n      .publishReplay(1) // Use refCount to simplify handling multiple subscribers. The creation function will\n      // be called when the first subscriber subscribes and unsubscribe will be called when\n      // the last subscriber unsubscribes.\n      .refCount();\n      return _this.observableCache[sid];\n    });\n\n    _defineProperty(this, \"unqueueNextFetch\", function () {\n      if (_this.heartbeatSubscription) {\n        _this.heartbeatSubscription.unsubscribe();\n\n        _this.heartbeatSubscription = null;\n      }\n    });\n\n    _defineProperty(this, \"queueNextFetch\", function () {\n      _this.unqueueNextFetch(); // Only queue a fetch if there are subscribers.\n\n\n      if (!(0, _isEmpty[\"default\"])(_this.searchSubscribers)) {\n        _this.heartbeatSubscription = _this.createHeartbeat().subscribe(_this.handleProgress);\n      }\n    });\n\n    _defineProperty(this, \"handleProgress\", function (res) {\n      if (!res || !(0, _isArray[\"default\"])(res.entry)) {\n        if (true) {\n          // eslint-disable-next-line no-console\n          console.log(new Error('Unrecognized response.'));\n        }\n      } else {\n        res.entry.forEach(function (entry) {\n          var sid = entry.content.sid; // Check that the subscriber is still subscribed. It could have\n          // unsubscribed between the fetch call and response.\n\n          if (_this.searchSubscribers[sid]) {\n            _this.searchSubscribers[sid].next(entry);\n          } // Check that the subscriber is still subscribed. It could have\n          // unsubscribed in the `next` call.\n\n\n          if (_this.searchSubscribers[sid]) {\n            // A search is complete when it is done or has been finalized\n            if (entry.content.isDone || entry.content.isFinalized) {\n              _this.searchSubscribers[sid].complete();\n            }\n          }\n        }); // Reconcile returned searches with requested ones and trigger an error for\n        // searches that were requested but not returned.\n\n        var returnedSearches = res.entry.map(function (entry) {\n          return entry.content.sid;\n        });\n        (0, _difference[\"default\"])(_this.requestedSearches, returnedSearches).forEach(function (sid) {\n          if (_this.searchSubscribers[sid]) {\n            _this.searchSubscribers[sid].error(new Error(\"Search \".concat(sid, \" not found. The search may have been cancelled while there are still subscribers.\")));\n          }\n        });\n      }\n    });\n\n    this.initialize();\n  }\n  /**\n   * Initializes the instance. Used by the constructor and resetTest.\n   * @private\n   */\n\n\n  _createClass(ProgressTracker, [{\n    key: \"initialize\",\n    value: function initialize() {\n      this.unqueueNextFetch();\n      this.longInterval = 1000;\n      this.shortInterval = 100;\n      this.heartbeatSubscription = null;\n      this.requestedSearches = null;\n      this.searchSubscribers = {};\n      this.observableCache = {};\n      this.splunkConfig = undefined;\n      this.baseFetchInit = undefined;\n    }\n    /**\n     * Set the Splunk config values used for requests to the server.\n     *\n     * @param {Object} [splunkConfig]\n     * @public\n     */\n\n  }, {\n    key: \"createHeartbeat\",\n\n    /**\n     * Fetches the searches from the server and emits the appropriate response the the appropriate\n     * observers.\n     *\n     * TODO: Consider accepting a TestScheduler here that would be passed to `Observable.interval`.\n     * This could simplify the unit tests.\n     *\n     * @param interval {Number} - The polling interval.\n     * @private\n     */\n    value: function createHeartbeat() {\n      var _this2 = this;\n\n      return _rxjs.Observable.timer(this.shortInterval, this.longInterval) // Make a request for the current jobs each heart beat. Use exhaustMap to avoid the\n      // unlikely situation where the response from the server takes longer than the\n      // interval length.\n      .exhaustMap(function () {\n        if ((0, _isEmpty[\"default\"])(_this2.searchSubscribers)) {\n          return _rxjs.Observable.empty();\n        }\n\n        _this2.requestedSearches = (0, _keys[\"default\"])(_this2.searchSubscribers);\n        return _rxjs.Observable.fromPromise((0, _search.batchGetSearches)(_this2.requestedSearches, {}, _this2.splunkConfig, _this2.baseFetchInit));\n      }) // Something went wrong with the fetch. Log the error to the console, but don't\n      // throw. Continue polling next inerval.\n      [\"catch\"](function (err, caught) {\n        if (true) {\n          // eslint-disable-next-line no-console\n          console.log(err);\n        }\n\n        return caught;\n      });\n    }\n  }]);\n\n  return ProgressTracker;\n}();\n\nvar progressTracker = new ProgressTracker();\nvar resetTest = progressTracker.resetTest,\n    setBaseFetchInit = progressTracker.setBaseFetchInit,\n    setIntervalTest = progressTracker.setIntervalTest,\n    setSplunkConfig = progressTracker.setSplunkConfig,\n    track = progressTracker.track;\nexports.track = track;\nexports.setSplunkConfig = setSplunkConfig;\nexports.setIntervalTest = setIntervalTest;\nexports.setBaseFetchInit = setBaseFetchInit;\nexports.resetTest = resetTest;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/search-job/progressTracker.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js":
/*!*******************************************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js ***!
  \*******************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.getCSRFToken = getCSRFToken;\nexports.extractAppName = extractAppName;\nexports.versionLabel = exports.username = exports.splunkdPath = exports.serverTimezoneInfo = exports.rootPath = exports.portNumber = exports.locale = exports.config = exports.buildPushNumber = exports.buildNumber = exports.appBuild = exports.app = exports.isAvailable = exports.CSRFToken = void 0;\nvar _get = _interopRequireDefault(__webpack_require__(/*! lodash/get */ \"./node_modules/lodash/get.js\"));\nvar _cookie = __webpack_require__(/*! @splunk/ui-utils/cookie */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/node_modules/@splunk/ui-utils/cookie.js\");\nfunction _interopRequireDefault(e) { return e && e.__esModule ? e : { \"default\": e }; }\n/**\n * A set of constants describing the server environment. If window.$C isn't defined, most of the\n * values on this page are also undefined.\n *\n * The constants may be imported independently, such as:\n * ```\n * import { buildNumber, locale, rootPath } from '@splunk/splunk-utils/config';\n * @file\n */\n\n/* global global */\n// If this is running in Node, the root is the `global` object. If it's running in the browser,\n// root is the `window` object.\nvar root = typeof window === 'undefined' ? __webpack_require__.g : window;\n\n/**\n * A function to get a $C prop. Returns undefined if $C isn't available.\n * @private\n */\nfunction get$CEntry(prop) {\n  return (0, _get[\"default\"])(root, ['$C', prop]);\n}\n\n/**\n * The CSRF token. This may be `null` if the token isn't found in the cookie.\n * @deprecated `CSRFToken` is deprecated and will be removed in a future major version. Use `getCSRFToken` instead.\n * @constant\n *\n * @public\n */\nvar CSRFToken = (0, _cookie.getEntry)(\"splunkweb_csrf_token_\".concat(get$CEntry('MRSPARKLE_PORT_NUMBER')));\n\n/**\n * A function to get the CSRF token. This may be `null` if the token isn't found in the cookie.\n * @returns {string} The current CSRF token stored in cookies.\n * @public\n */\nexports.CSRFToken = CSRFToken;\nfunction getCSRFToken() {\n  return (0, _cookie.getEntry)(\"splunkweb_csrf_token_\".concat(get$CEntry('MRSPARKLE_PORT_NUMBER')));\n}\n\n/**\n * If `window.$C` is undefined or invalid, isAvailable is `false`.\n * @constant\n * @public\n */\nvar isAvailable = !!get$CEntry('SPLUNKD_PATH');\n\n/**\n * Extracts app name from the URL. To enable testing, this function is exported and pathname\n * can be overridden.\n * @private\n */\nexports.isAvailable = isAvailable;\nfunction extractAppName() {\n  var pathname = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : (0, _get[\"default\"])(root, ['document', 'location', 'pathname'], '');\n  var pathMatch = pathname.match(/\\w\\w.\\w\\w\\/(app|manager)\\/([^/]+)/);\n  return pathMatch ? pathMatch[2] : undefined;\n}\n\n/**\n * The name of the current app as shown in the URL (not the display name).\n * @constant\n * @public\n */\nvar app = extractAppName();\n\n/**\n * The build number of the current app.\n * @constant\n * @public\n */\nexports.app = app;\nvar appBuild = get$CEntry('APP_BUILD');\n\n/**\n * A hash string of the current build.\n * @constant\n * @type {string}\n * @public\n */\nexports.appBuild = appBuild;\nvar buildNumber = get$CEntry('BUILD_NUMBER');\n\n/**\n * A incremented number configured per instance. Defaults to 0.\n * @constant\n * @public\n */\nexports.buildNumber = buildNumber;\nvar buildPushNumber = get$CEntry('BUILD_PUSH_NUMBER');\n\n/**\n * The full object of window.$C.\n * @constant\n * @public\n */\nexports.buildPushNumber = buildPushNumber;\nvar config = (0, _get[\"default\"])(root, '$C');\n\n/**\n * The current locale, such as \"en-US\".\n * @constant\n * @public\n */\nexports.config = config;\nvar locale = get$CEntry('LOCALE');\n\n/**\n * The port that Splunk Web is using. Defaults to 8000.\n * @constant\n * @public\n */\nexports.locale = locale;\nvar portNumber = get$CEntry('MRSPARKLE_PORT_NUMBER');\n\n/**\n * Splunk may not be running in the root directory, so the locale may not be the first element\n * in the pathname. If it is running in the root directory, ```rootPath``` is an empty\n * string. Otherwise it returns the root. For example, in the page\n * ```\n * http://splunk.domain.com/my-root-dir/en-US/app/search/search\n * ```\n * ```rootPath``` is set to ```'my-root-dir'```.\n * @constant\n * @public\n */\nexports.portNumber = portNumber;\nvar rootPath = get$CEntry('MRSPARKLE_ROOT_PATH');\n\n/**\n * The serialized Splunk Enterprise time zone data. This can be parsed for use in formatting Epoch times\n * on the client.\n * @constant\n * @public\n */\nexports.rootPath = rootPath;\nvar serverTimezoneInfo = get$CEntry('SERVER_ZONEINFO');\n\n/**\n * The Splunk Web proxing to splunkd, such as ```/en-US/splunkd/__raw```.\n * @constant\n * @public\n */\nexports.serverTimezoneInfo = serverTimezoneInfo;\nvar splunkdPath = get$CEntry('SPLUNKD_PATH');\n\n/**\n * The username of the current user.\n * @constant\n * @public\n */\nexports.splunkdPath = splunkdPath;\nvar username = get$CEntry('USERNAME');\n\n/**\n * The version label of splunkd.\n * @constant\n * @public\n */\nexports.username = username;\nvar versionLabel = get$CEntry('VERSION_LABEL');\nexports.versionLabel = versionLabel;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/fetch.js":
/*!******************************************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/fetch.js ***!
  \******************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.getDefaultFetchInit = getDefaultFetchInit;\nexports.createFetchInit = createFetchInit;\nexports.findErrorMessage = findErrorMessage;\nexports.handleResponse = handleResponse;\nexports.handleError = handleError;\nexports.defaultFetchInit = void 0;\nvar _find = _interopRequireDefault(__webpack_require__(/*! lodash/find */ \"./node_modules/lodash/find.js\"));\nvar _includes = _interopRequireDefault(__webpack_require__(/*! lodash/includes */ \"./node_modules/lodash/includes.js\"));\nvar _config = __webpack_require__(/*! ./config */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js\");\nfunction _interopRequireDefault(e) { return e && e.__esModule ? e : { \"default\": e }; }\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }\nfunction _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }\nfunction _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\n/**\n * A collection of utils for making [fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch)\n * requests to the [Splunk REST API](http://docs.splunk.com/Documentation/Splunk/latest/RESTREF/RESTprolog)\n * and working with the responses.\n * @file\n */\n\n/**\n * A default initialization object for fetch requests. It includes the required credentials and headers.\n * @deprecated `defaultFetchInit` is deprecated and will be removed in a future major version. Use `getDefaultFetchInit` instead.\n * @constant\n * @public\n */\nvar defaultFetchInit = {\n  method: 'GET',\n  credentials: 'include',\n  headers: {\n    'X-Splunk-Form-Key': _config.CSRFToken,\n    'X-Requested-With': 'XMLHttpRequest',\n    'Content-Type': 'application/x-www-form-urlencoded'\n  }\n};\n\n/**\n * A function to get the default initialization object for fetch requests. It includes the required credentials and headers.\n * @returns {RequestInit} The default fetch request headers and credentials.\n * @public\n */\nexports.defaultFetchInit = defaultFetchInit;\nfunction getDefaultFetchInit() {\n  return {\n    method: 'GET',\n    credentials: 'include',\n    headers: {\n      'X-Splunk-Form-Key': (0, _config.getCSRFToken)(),\n      'X-Requested-With': 'XMLHttpRequest',\n      'Content-Type': 'application/x-www-form-urlencoded'\n    }\n  };\n}\n\n/**\n * A function to create an initialization object for fetch requests. It includes the required credentials and headers.\n * @param {RequestInit} overrides - the options to override from the defaults.\n * @returns {RequestInit} The default fetch request headers and credentials.\n * @public\n */\nfunction createFetchInit(overrides) {\n  var defaults = getDefaultFetchInit();\n  return _objectSpread(_objectSpread(_objectSpread({}, defaults), overrides || {}), {}, {\n    headers: _objectSpread(_objectSpread({}, defaults.headers), (overrides === null || overrides === void 0 ? void 0 : overrides.headers) || {})\n  });\n}\n\n/**\n * Finds an error message, if available, in a Splunk REST API response.\n * @param {Object} response - The response from the Splunk server.\n * @returns {Object|undefined} The error message object, or undefined if no error is found.\n * @public\n */\n// eslint-disable-next-line consistent-return\nfunction findErrorMessage() {\n  var _ref = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},\n    messages = _ref.messages;\n  if (Array.isArray(messages)) {\n    return (0, _find[\"default\"])(messages, function (_ref2) {\n      var type = _ref2.type;\n      return type === 'ERROR' || type === 'FATAL' || type === 'risky_command';\n    });\n  }\n}\n\n/**\n * Rejects responses with a status different than the expected. Otherwise, resolves with the JSON\n * response or `null` if the response status code is 204.\n *\n * @param {Number|Number[]} expectedStatus - The status code of a successful response.\n * @returns {function(Response): Promise<any>} A response handler function.\n * @public\n */\nfunction handleResponse(expectedStatus) {\n  return function (res) {\n    if ((0, _includes[\"default\"])([].concat(expectedStatus), res.status)) {\n      return res.status === 204 ? null : res.json();\n    }\n    return Promise.reject(res);\n  };\n}\n\n/**\n * Handles a failed Splunk REST API response by looking for an error message in the response. Returns\n * a rejection for further handling by the end consumer.\n *\n * @param {String} defaultMessage - This is used if a message from the server\n * is not found or if the response from the server couldn't be parsed as JSON.\n * @returns {function(Response): Promise<never>} A response handler function.\n * @public\n */\nfunction handleError(defaultMessage) {\n  return function (res) {\n    var dfd = null;\n    try {\n      dfd = res.json();\n    } catch (e) {\n      // If body is not JSON\n      return Promise.reject(new Error(defaultMessage));\n    }\n    return dfd.then(function (data) {\n      // If the action failed, most of the time you get an error message.\n      var errorMessage = findErrorMessage(data);\n      if (errorMessage) {\n        return Promise.reject(new Error(errorMessage.text));\n      }\n      // Sometimes there is no error message.\n      return Promise.reject(new Error(defaultMessage));\n    });\n  };\n}\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/fetch.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/node_modules/@splunk/ui-utils/cookie.js":
/*!*************************************************************************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/node_modules/@splunk/ui-utils/cookie.js ***!
  \*************************************************************************************************************************/
/***/ (function(__unused_webpack_module, exports) {

eval("\n/* eslint-disable import/prefer-default-export */\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.getEntry = getEntry;\n/**\n * Retrieves the cookie value for the reference name passed in.\n * This can be `null` if the token or the reference name is not found in the cookie.\n *\n * For example:\n * ```\n *    const CSRFToken = getEntry('splunkweb_csrf_token_8000');\n * ```\n *\n * @param {String} name Reference to the name in the cookie.\n *\n * @returns {String|null} If found, returns the cookie value associated with the name; null otherwise.\n *\n * @public\n *\n */\nfunction getEntry(name) {\n    if (typeof document === 'undefined') {\n        return null;\n    }\n    const regex = new RegExp(`(^|; ?)${name}=([^;]+)`);\n    let value = null;\n    try {\n        const match = document.cookie.match(regex);\n        value = match ? match[2] : null;\n    }\n    catch (e) {\n        // no-op, let `value` remain null\n    }\n    return value;\n}\n\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/node_modules/@splunk/ui-utils/cookie.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/url.js":
/*!****************************************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/url.js ***!
  \****************************************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.withConfig = withConfig;\nexports.insertCacheBuster = exports.createURL = exports.createStaticURL = exports.createRESTURL = exports.createDocsURL = exports.createAppDocsURL = void 0;\nvar _querystring = __webpack_require__(/*! querystring */ \"./node_modules/querystring/index.js\");\nvar config = _interopRequireWildcard(__webpack_require__(/*! ./config */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js\"));\nfunction _interopRequireWildcard(e, t) { if (\"function\" == typeof WeakMap) var r = new WeakMap(), n = new WeakMap(); return (_interopRequireWildcard = function _interopRequireWildcard(e, t) { if (!t && e && e.__esModule) return e; var o, i, f = { __proto__: null, \"default\": e }; if (null === e || \"object\" != _typeof(e) && \"function\" != typeof e) return f; if (o = t ? n : r) { if (o.has(e)) return o.get(e); o.set(e, f); } for (var _t in e) { \"default\" !== _t && {}.hasOwnProperty.call(e, _t) && ((i = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e, _t)) && (i.get || i.set) ? o(f, _t, i) : f[_t] = e[_t]); } return f; })(e, t); }\nfunction _typeof(o) { \"@babel/helpers - typeof\"; return _typeof = \"function\" == typeof Symbol && \"symbol\" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && \"function\" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? \"symbol\" : typeof o; }, _typeof(o); }\nfunction ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }\nfunction _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }\nfunction _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }\nfunction _toPropertyKey(t) { var i = _toPrimitive(t, \"string\"); return \"symbol\" == _typeof(i) ? i : i + \"\"; }\nfunction _toPrimitive(t, r) { if (\"object\" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || \"default\"); if (\"object\" != _typeof(i)) return i; throw new TypeError(\"@@toPrimitive must return a primitive value.\"); } return (\"string\" === r ? String : Number)(t); }\n/**\n * A set of functions for generating Splunk Web URLs. They may be imported independently, such as:\n * ```\n * import { createURL, createStaticURL } from '@splunk/splunk-utils/url';\n * ```\n * @file\n */\n\nfunction withConfig(_ref) {\n  var appBuild = _ref.appBuild,\n    buildNumber = _ref.buildNumber,\n    buildPushNumber = _ref.buildPushNumber,\n    locale = _ref.locale,\n    rootPath = _ref.rootPath,\n    splunkdPath = _ref.splunkdPath;\n  /**\n   * Inserts a cache buster into the given path.\n   *\n   * @param path {String} - The path to inject the cache buster into.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`, and\n   * `buildNumber`. Defaults to the values provided by `@splunk/splunk-utils/config`.\n   * @return {String} Returns the path with the cache buster `/@9850fc87a5ee`.\n   * @private\n   */\n  function insertCacheBuster(path) {\n    var configOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n    var options = _objectSpread({\n      appBuild: appBuild,\n      buildPushNumber: buildPushNumber,\n      buildNumber: buildNumber\n    }, configOptions);\n    var match = path.match(/(^|\\w\\w-\\w\\w\\/)static\\//);\n    if (!match) {\n      return path;\n    }\n    var insertPosition = match.index + match[0].length - 1;\n    var isApp = path.match(/(^|\\w\\w-\\w\\w|)static\\/app/);\n    var appBuildString = isApp ? \":\".concat(options.appBuild || 0) : '';\n    var buildPushString = options.buildPushNumber ? \".\".concat(options.buildPushNumber) : '';\n    var cacheBusterString = \"/@\".concat(options.buildNumber).concat(buildPushString).concat(appBuildString);\n    var before = path.substr(0, insertPosition);\n    var after = path.substr(insertPosition);\n    return \"\".concat(before).concat(cacheBusterString).concat(after);\n  }\n\n  /**\n   * Returns a full path for the current appserver location.\n   * This is required for proxy compatiblity, localization, and cache busting.\n   *\n   * For example:\n   * ```js\n   * createURL('app/search/job'); // \"/splunk/en-US/app/search/job\"\n   * createURL('/app/search/job'); // \"/splunk/en-US/app/search/job\"\n   * createURL('app/search/job', {id:3, user:'me' }); // \"/splunk/en-US/app/search/job?id=3&user=me\"\n   * ```\n   *\n   * Static paths are augmented with a cache buster:\n   * ```js\n   * createURL('/static/js/foo.js'); // \"/splunk/en-US/static/@12345/js/foo.js\"\n   * createURL('/static/js/foo.js'); // \"/splunk/en-US/static/@12345.1/js/foo.js\"\n   * createURL('/static/app/search/js/foo.js'); // \"/splunk/en-US/static/@12345.1:0/app/search/js/foo.js\"\n   * ```\n   *\n   * @public\n   * @param path {String}\n   * @param [queryParams] {Object} An object of the query parameters.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`,\n   * `buildNumber`, `rootPath`, and `locale`. Defaults to the values provided by\n   * `@splunk/splunk-utils/config`.\n   * @return {String} Returns a valid URL with correct root path, locale, and cache buster.\n   * @alias createURL\n   */\n  function createURL(pathInput, queryParams) {\n    var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n    var options = _objectSpread({\n      appBuild: appBuild,\n      buildPushNumber: buildPushNumber,\n      buildNumber: buildNumber,\n      rootPath: rootPath,\n      locale: locale\n    }, configOptions);\n    var path = pathInput || '/';\n    var query = queryParams ? \"?\".concat((0, _querystring.stringify)(queryParams)) : '';\n    if (path.charAt(0) !== '/') {\n      path = \"/\".concat(path);\n    }\n    path = \"\".concat(options.rootPath || '', \"/\").concat(options.locale).concat(path).concat(query);\n    return insertCacheBuster(path, options);\n  }\n\n  /**\n   * Prepends Splunk Web path information necessary to access a static asset. For example:\n   * ```\n   * createStaticURL('fonts/roboto-regular-webfont.woff'); // \"/splunk/en-US/static/@12345/fonts/roboto-regular-webfont.woff\"\n   * createStaticURL('app/search/fonts/roboto-regular-webfont.woff'); // \"/splunk/en-US/static/@12345.1:0/app/search/fonts/roboto-regular-webfont.woff\"\n   * ```\n   * @public\n   * @param {String} path - A path to a static resource.\n   * @param [queryParams] {Object} An object of the query parameters.\n   * @param configOptions {Object} - Config options including `appBuild`, `buildPushNumber`,\n   * `buildNumber`, `rootPath`, and `locale`. Defaults to the values provided by\n   * `@splunk/splunk-utils/config`.\n   * @return {String} Returns a valid URL with correct root path, locale, and cache buster.\n   * @alias createStaticURL\n   */\n  function createStaticURL(path) {\n    for (var _len = arguments.length, rest = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {\n      rest[_key - 1] = arguments[_key];\n    }\n    return createURL.apply(void 0, [\"static/\".concat(path)].concat(rest));\n  }\n\n  /**\n   * Creates a fully qualified URL for the specified endpoint.\n   * For example:\n   * ```\n   * createRESTURL('server/info'); // \"/en-US/splunkd/__raw/services/server/info\"\n   * createRESTURL('saved/searches', {app: 'search'}); // \"/en-US/splunkd/__raw/servicesNS/-/search/saved/searches\"\n   * ```\n   * @public\n   * @param {String} endpoint - An endpoint to a REST API.\n   * @param {Object} options\n   * @param {String} [options.app] - App namespace to use.\n   * @param {String} [options.owner] - User namespace to use.\n   * @param {Sting} [options.sharing] - Must be one of [ '' | 'app' | 'global' | 'system' ].\n   * @param {Object} [configOptions] - Config options including `splunkdPath`. Defaults to the value\n   *  provided by `@splunk/splunk-utils/config`.\n   * @return {String} The URL of the REST API endpoint.\n   * @alias createRESTURL\n   */\n  function createRESTURL(endpoint) {\n    var namespaceOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};\n    var configOptions = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};\n    // Return the endpoint if it's a full path.\n    if (/^http[s]?:\\/\\//.test(endpoint)) {\n      return endpoint;\n    }\n    var basePath = configOptions.splunkdPath || splunkdPath || '';\n\n    // If it's an absolute path,\n    if (/^\\/.*/.test(endpoint)) {\n      // Append the splunkdPath if the endpoint includes services. Otherwise, return the endpoint.\n      return /^\\/services/.test(endpoint) ? \"\".concat(basePath).concat(endpoint) : endpoint;\n    }\n\n    // If neither an app nor owner is provided, use /services/ endpoint.\n    if (!namespaceOptions.app && !namespaceOptions.owner) {\n      return \"\".concat(basePath, \"/services/\").concat(endpoint);\n    }\n\n    // Build a namespaced path.\n    var owner;\n    if (namespaceOptions.sharing) {\n      owner = 'nobody';\n    } else if (namespaceOptions.owner) {\n      owner = encodeURIComponent(namespaceOptions.owner);\n    } else {\n      owner = '-';\n    }\n    var app;\n    if (namespaceOptions.sharing === 'system') {\n      app = 'system';\n    } else if (namespaceOptions.app) {\n      app = encodeURIComponent(namespaceOptions.app);\n    } else {\n      app = '-';\n    }\n    return \"\".concat(basePath, \"/servicesNS/\").concat(owner, \"/\").concat(app, \"/\").concat(endpoint);\n  }\n\n  /** Create docs URL private method.\n   * @param {Object} [params]\n   * @private\n   */\n  function createDocsURLFromParams(params, configOptions) {\n    return createURL('/help', params, configOptions);\n  }\n\n  /** Create docs URL using location string.\n   * For example:\n   * ```\n   * createDocsURL('learnmore.timerange.picker'); // \"/en-US/help?location=learnmore.timerange.picker\"\n   * ```\n   * @public\n   * @param {String} location - Required.\n   * @return {String} The URL of the Splunk help document.\n   * @alias createDocsURL\n   */\n  function createDocsURL(location, configOptions) {\n    return createDocsURLFromParams({\n      location: location\n    }, configOptions);\n  }\n\n  /** Create app docs URL using location string, which needs to add [appName:appVersion] prefix before the location string.\n   * For example:\n   * ```\n   * createAppDocsURL('learnmore.timerange.picker', { appName: 'splunk_app_aws', appVersion: '5.0.3'}); // \"/en-US/help?location=learnmore.timerange.picker\"\n   * ```\n   * @public\n   * @param {String} location - Required.\n   * @param {Object} options - Required.\n   * @param {String} options.appName - App name.\n   * @param {String} options.appVersion - App version.\n   * @return {String} The URL of the Splunk app help document.\n   * @alias createAppDocsURL\n   */\n  function createAppDocsURL(location, _ref2, configOptions) {\n    var appName = _ref2.appName,\n      appVersion = _ref2.appVersion;\n    return createDocsURLFromParams({\n      location: \"[\".concat(appName, \":\").concat(appVersion, \"]\").concat(location)\n    }, configOptions);\n  }\n  return {\n    createAppDocsURL: createAppDocsURL,\n    createDocsURL: createDocsURL,\n    createRESTURL: createRESTURL,\n    createStaticURL: createStaticURL,\n    createURL: createURL,\n    insertCacheBuster: insertCacheBuster\n  };\n}\nvar _withConfig = withConfig(config),\n  createAppDocsURL = _withConfig.createAppDocsURL,\n  createDocsURL = _withConfig.createDocsURL,\n  createRESTURL = _withConfig.createRESTURL,\n  createStaticURL = _withConfig.createStaticURL,\n  createURL = _withConfig.createURL,\n  insertCacheBuster = _withConfig.insertCacheBuster;\nexports.insertCacheBuster = insertCacheBuster;\nexports.createURL = createURL;\nexports.createStaticURL = createStaticURL;\nexports.createRESTURL = createRESTURL;\nexports.createDocsURL = createDocsURL;\nexports.createAppDocsURL = createAppDocsURL;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/url.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/time.js":
/*!*******************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/time.js ***!
  \*******************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nvar _interopRequireDefault = __webpack_require__(/*! @babel/runtime/helpers/interopRequireDefault */ \"./node_modules/@babel/runtime/helpers/interopRequireDefault.js\");\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.normalizeUnit = normalizeUnit;\nexports.normalizeSnapUnit = normalizeSnapUnit;\nexports.getUnitLabel = getUnitLabel;\nexports.removeRealTime = removeRealTime;\nexports.removeISOTimezone = removeISOTimezone;\nexports.isEpoch = isEpoch;\nexports.isISO = isISO;\nexports.isAbsolute = isAbsolute;\nexports.getISOWithTimeZone = getISOWithTimeZone;\nexports.parseTimeString = parseTimeString;\nexports.isValidTime = isValidTime;\nexports.isRealTime = isRealTime;\nexports.isWholeDay = isWholeDay;\nexports.isEarliestEmpty = isEarliestEmpty;\nexports.isLatestNow = isLatestNow;\nexports.isAllTime = isAllTime;\nexports.timeRangesAreEquivalent = timeRangesAreEquivalent;\nexports.findPresetLabel = findPresetLabel;\nexports.createRangeLabel = createRangeLabel;\nexports.formatDuration = formatDuration;\nexports.getAcceleratedTimeRange = exports.getRealTimeEarliest = exports.getRelativeLatest = exports.getRelativeEarliest = exports.getToSnap = exports.getFromUnit = exports.getFromSnap = exports.getFromNumber = void 0;\n\nvar _lodash = __webpack_require__(/*! lodash */ \"./node_modules/lodash/lodash.js\");\n\nvar _format = __webpack_require__(/*! @splunk/ui-utils/format */ \"./node_modules/@splunk/ui-utils/format.js\");\n\nvar _config = __webpack_require__(/*! @splunk/splunk-utils/config */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/config.js\");\n\nvar _i18n = __webpack_require__(/*! @splunk/ui-utils/i18n */ \"./node_modules/@splunk/ui-utils/i18n.js\");\n\nvar _moment = _interopRequireDefault(__webpack_require__(/*! @splunk/moment */ \"./node_modules/@splunk/moment/index.js\"));\n\nvar _timeUnits = __webpack_require__(/*! ./timeUnits */ \"./node_modules/@splunk/time-range-utils/timeUnits.js\");\n\n/**\n * Utilities for interpreting, parsing, and validating time ranges. Each function can be imported\n * separately.\n * ```\n *  import { normalizeUnit, createRangeLabel } from '@splunk/time-range-utils/time'\n * ```\n *\n * @file\n */\n\n/**\n * Normalizes units to it's shortest version, such as `s` for `sec` and `mon` for `month`.\n * @name normalizeUnit\n * @function\n * @public\n * @param {string} unit - The unit, such as `s` or `quarter`.\n * @param {bool} [removeInvalid=true] - When true, returns an empty string for invalid units,\n * when false\n * returns 's' for invalid units.\n * @returns {string} Returns the normalized unit or empty string.\n */\nfunction normalizeUnit(abbr, removeInvalid = true) {\n  const hasUnit = (0, _lodash.has)(_timeUnits.timeUnits, abbr);\n  const defaultUnit = removeInvalid ? '' : _timeUnits.timeUnits.s.abbr;\n  return hasUnit ? _timeUnits.timeUnits[abbr].abbr : defaultUnit;\n}\n/**\n * Normalizes snap units to it's shortest version, this is the same as normalizeUnit, but also\n * supports weekdays, such as `w5`.\n * @public\n * @param {string} unit - The unit, such as `s`, `quarter` or `w0`.\n * @param {bool} [removeInvalid=true] - When true, returns an empty string for invalid units,\n * when false returns 's' for invalid units.\n * @returns {string} Returns the normalized unit or empty string.\n */\n\n\nfunction normalizeSnapUnit(abbr, removeInvalid = true) {\n  const hasUnit = (0, _lodash.has)(_timeUnits.snapUnits, abbr);\n  const defaultUnit = removeInvalid ? '' : _timeUnits.timeUnits.s.abbr;\n  return hasUnit ? _timeUnits.snapUnits[abbr].abbr : defaultUnit;\n}\n/**\n * Returns a label for a unit abbreviation, such as 'second' for 's' or 'sec'.\n * @public\n * @param {object} unit -  The unit, such as `s`, `quarter` or `w0`.\n * @param {object} [plural=false] - Whether the returned label should be plural.\n * @returns {string}\n */\n\n\nfunction getUnitLabel(unit, plural = false) {\n  return _timeUnits.snapUnits[unit][plural ? 'plural' : 'singular'];\n}\n/**\n * Strips rt from the beginning of a time string when found. This makes a time string compatible\n * with the time parser. To ensure capability with the time parser 'rt' returns 'now'.\n * @public\n * @param {string} time - The time string such as `47165491` or `rt-2h@m`.\n * @returns {string} Returns the time string.\n */\n\n\nfunction removeRealTime(time) {\n  if (time === 'rt') {\n    return 'now';\n  }\n\n  return time.replace(/^rt/, '');\n}\n/**\n * Removes the timezone from an iso time string\n * @public\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {string}\n */\n\n\nfunction removeISOTimezone(time) {\n  return time.replace(/[+-]\\d?\\d:\\d\\d$/, '');\n}\n/**\n * Validates that a string represents a unix epoch time.\n * @public\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isEpoch(time) {\n  return (0, _lodash.isFinite)(time) || (0, _lodash.isString)(time) && /^\\d+((\\.\\d+)|(\\d*))$/.test(time);\n}\n/**\n * Validates that a string represents an ISO time.\n * @public\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isISO(time) {\n  return !!time.match(/^\\d\\d\\d\\d-\\d\\d?-\\d\\d?T\\d\\d?:\\d\\d?(:\\d\\d?)?(\\.\\d\\d?\\d?)?([+-]\\d\\d?:\\d\\d|Z)?$/);\n}\n/**\n * Validates that a string represents an ISO or unix epoch time.\n * @public\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isAbsolute(time) {\n  if (time === undefined) {\n    return false;\n  }\n\n  return isEpoch(time) || isISO(time);\n}\n/**\n * Adds timezone to ISO time.\n * @public\n * @param {string} time - The time string such as “2020-09-17T09:44:12.404”.\n * @param {string} [momentTimeZoneName]\n * @returns {string} - ISO with timezone “2020-09-17T16:44:12.404Z”\n */\n\n\nfunction getISOWithTimeZone(time, momentTimeZoneName) {\n  if (!(0, _lodash.isString)(time)) {\n    return null;\n  }\n\n  if (momentTimeZoneName) {\n    return _moment.default.tz(time, momentTimeZoneName).toISOString();\n  }\n\n  return (0, _moment.default)(time).toISOString();\n}\n/**\n * Parses a time string for inspection or form population.\n *\n * Example parse for a relative time string:\n * ```\n * {\n *     string: '-3d@qtr+2hr',\n *     type: ['relative'], // 'relative', 'realTime', 'iso', or 'epoch'\n *     isFullyParsed: true,\n *     modifiers: [\n *         {\n *             string: '-3d@qtr',\n *             isParsed: true,\n *             unit: 'd',\n *             amount: -3,\n *             snap: 'q',\n *         },\n *         {\n *             string: '+2hr',\n *             isParsed: true,\n *             unit: 'h',\n *             amount: +2,\n *             snap: false,\n *         },\n *     ],\n * }\n * ```\n * Example parse for a epoch time:\n * ```\n * {\n *     string: '89451357',\n *     type: ['epoch'],\n *     isFullyParsed: true,\n *     modifiers: [],\n * }\n *\n * @public\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {object}\n */\n\n\nfunction parseTimeString(timeString) {\n  if (!(0, _lodash.isString)(timeString)) {\n    return false;\n  }\n\n  const ast = {\n    string: timeString,\n    isFullyParsed: true,\n    modifiers: []\n  };\n\n  if (isISO(timeString)) {\n    ast.type = 'iso';\n    return ast;\n  }\n\n  if (isEpoch(timeString)) {\n    ast.type = 'epoch';\n    return ast;\n  }\n\n  const segments = timeString.trim().replace(/-/g, '\\n-').replace(/\\+/g, '\\n+').split('\\n'); // If only snap has been provided push empty string to beginning of segments.\n\n  if (segments[0].charAt(0) === '@') {\n    segments.unshift('');\n  }\n\n  if (segments[0] === 'rt' || segments[0] === 'rtnow') {\n    ast.type = 'realTime';\n  } else if (['rt', 'now', 'rtnow', ''].indexOf(segments[0]) < 0) {\n    ast.isFullyParsed = false;\n    return ast;\n  } else {\n    ast.type = 'relative';\n  }\n\n  ast.modifiers = segments.slice(1).map(segmentString => {\n    const segment = {\n      string: segmentString,\n      isParsed: false\n    };\n    const sParse = segmentString.match(/^(([-+]\\d*)([a-zA-Z]*))?(@([a-zA-Z][a-zA-Z0-7]*))?$/); //                                          2         3      4          5\n\n    if (sParse) {\n      segment.unit = sParse[3] && normalizeUnit(sParse[3], false);\n      segment.amount = segment.unit ? parseInt(sParse[2], 10) || 1 : 0;\n      segment.snap = !!sParse[4] && normalizeSnapUnit(sParse[5], false);\n      segment.isParsed = true;\n\n      if (sParse[5] && !_timeUnits.snapUnits[sParse[5]] || sParse[3] && !_timeUnits.timeUnits[sParse[3]] || // invalid snap unit\n      segmentString.length === 1 // unqualified - or +\n      ) {\n        ast.isFullyParsed = false;\n        segment.isParsed = false;\n      }\n    } else {\n      ast.isFullyParsed = false;\n    }\n\n    return segment;\n  });\n  return ast;\n}\n/**\n * Validates that a string is a valid time string.\n * @public\n * @param {string} time - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isValidTime(time) {\n  return parseTimeString(time).isFullyParsed;\n}\n/**\n * Validates that a string represents a real-time search.\n * @public\n * @param {string} time - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isRealTime(time) {\n  const parse = parseTimeString(time);\n  return parse && parse.type === 'realTime' && parse.isFullyParsed;\n}\n/**\n * Validates that a iso time string is a whole day.\n * @public\n * @param {string|object} time - A time string (such as `2008-09-15T15:53:00+05:00`) or a\n * @splunk/moment time instance.\n * @returns {bool}\n */\n\n\nfunction isWholeDay(time) {\n  if ((0, _lodash.isString)(time) && isISO(time) || _moment.default.isMoment(time) && time.isValid()) {\n    const timeMoment = (0, _moment.default)(time);\n    return timeMoment.valueOf() === timeMoment.startOf('day').valueOf();\n  }\n\n  return false;\n}\n/**\n * Validate that a time string acts is either empty or `0`.\n * @public\n * @param {string} time - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isEarliestEmpty(time) {\n  if (time === '0') {\n    return true;\n  }\n\n  return !time;\n}\n/**\n * Validate that a time string acts is either empty or now\n * @public\n * @param {string} time - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isLatestNow(time) {\n  if (!time) {\n    return true;\n  }\n\n  return (0, _lodash.isString)(time) && time === 'now';\n}\n/**\n * Validate that a time range acts is equivalent to all-time.\n * @public\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {bool}\n */\n\n\nfunction isAllTime(earliest, latest) {\n  return isEarliestEmpty(removeRealTime(earliest)) && isLatestNow(removeRealTime(latest));\n}\n/**\n * Validate that two time range are equivalent. This normalizes the two comparisons using\n * isEarliestEmpty() and isLatestNow().\n * @public\n * @param {object} range1 - The time string range  such as `{ earliest: '-1d', latest: 'now' }`.\n * @param {object} range2 -  The time string range such as `{ earliest: '0', latest: '-1d' }`.\n * @returns {bool}\n */\n\n\nfunction timeRangesAreEquivalent(range1, range2) {\n  const earliest1 = range1.earliest;\n  const latest1 = range1.latest;\n  const earliest2 = range2.earliest;\n  const latest2 = range2.latest;\n  let earliestEqual = false;\n  let latestEqual = false;\n\n  if (isEarliestEmpty(earliest1) && isEarliestEmpty(earliest2)) {\n    earliestEqual = true;\n  } else {\n    earliestEqual = earliest1 === earliest2;\n  }\n\n  if (isLatestNow(latest1) && isLatestNow(latest2)) {\n    latestEqual = true;\n  } else {\n    latestEqual = latest1 === latest2;\n  }\n\n  return earliestEqual && latestEqual;\n}\n/**\n * Searches through an array of presets and returns any equivalent labels using\n * timeRangesAreEquivalent().\n * @public\n * @param {array} presets - An array of presents such as:\n * ```\n * [\n *     { label: '30 second window', earliest: 'rt-30s', latest: 'rt' },\n *     { label: 'Today', earliest: '@d', latest: 'now' },\n *     { label: 'Previous year', earliest: '-1y@y', latest: '@y' },\n *     { label: 'Last 15 minutes', earliest: '-15m', latest: 'now' },\n *     { label: 'All time', earliest: '0', latest: '' },\n * ]\n * ```\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {string | false } The matched preset label.\n */\n\n\nfunction findPresetLabel(presets, earliest, latest) {\n  /* JSDoc and eslint cannot agree on the appropriate format of this */\n\n  /* eslint-disable */\n  const currentPreset = (0, _lodash.find)(presets, preset => {\n    const range2 = {\n      earliest: preset.earliest,\n      latest: preset.latest\n    };\n    return timeRangesAreEquivalent({\n      earliest,\n      latest\n    }, range2);\n  });\n  /* eslint-enable */\n\n  return currentPreset ? currentPreset.label : false;\n}\n/**\n * Generates a label for a real-time time search.\n * For example:  '5 minute window' or 'Real-time'\n * @private\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {null|string}\n */\n\n\nfunction createRealTimeLabel(earliest, latest) {\n  if (isRealTime(earliest) || isRealTime(latest)) {\n    const earliestParse = parseTimeString(earliest);\n    const earliestModifier = earliestParse.modifiers[0];\n    const latestParse = parseTimeString(latest);\n    const labelTemplates = {\n      s: (0, _i18n._)('%(time)d second window'),\n      m: (0, _i18n._)('%(time)d minute window'),\n      h: (0, _i18n._)('%(time)d hour window'),\n      d: (0, _i18n._)('%(time)d day window'),\n      w: (0, _i18n._)('%(time)d week window'),\n      mon: (0, _i18n._)('%(time)d month window'),\n      q: (0, _i18n._)('%(time)d quarter window'),\n      y: (0, _i18n._)('%(time)d year window')\n    };\n\n    if (earliestModifier && earliestParse.type === 'realTime' && latestParse.type === 'realTime' && latestParse.modifiers.length === 0 && (0, _lodash.has)(labelTemplates, earliestModifier.unit) && earliestParse.modifiers.length === 1) {\n      return (0, _format.sprintf)(labelTemplates[earliestModifier.unit], {\n        time: Math.abs(earliestModifier.amount)\n      });\n    } // Other Real-Time.\n\n\n    return (0, _i18n._)('Real-time');\n  }\n\n  return false;\n}\n/**\n * Generates a label for a relative time search.\n * For example:  'Last 5 minutes'\n * @private\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {null|string}\n */\n\n\nfunction createRelativeTimeLabel(earliest, latest) {\n  const earliestParse = parseTimeString(earliest);\n  const earliestModifier = earliestParse.modifiers[0];\n  const latestParse = parseTimeString(latest);\n  const latestModifier = latestParse.modifiers[0];\n\n  if (earliestParse.type === 'realTime' || latestParse.type === 'realTime' || !earliestModifier || earliestParse.modifiers.length > 1 || latestParse.modifiers.length > 1) {\n    return false;\n  }\n\n  if (earliestModifier.amount < 0 && (!earliestModifier.snap || earliestModifier.unit === earliestModifier.snap) && (isLatestNow(latest) || latestModifier && !latestModifier.amount) && (!latestModifier || !latestModifier.snap || earliestModifier.unit === latestModifier.snap)) {\n    const amount = Math.abs(earliestModifier.amount);\n    const pluralKey = amount > 1 ? 'plural' : 'singular';\n    return (0, _format.sprintf)((0, _i18n._)('Last %(amount)d %(unit)s'), {\n      amount,\n      unit: _timeUnits.timeUnits[earliestModifier.unit][pluralKey]\n    });\n  }\n\n  return false;\n}\n/**\n * Compares two moment instances to determine if they are on the same day.\n * @private\n * @param {object} earliest - @splunk/moment time instance\n * @param {object} latest - @splunk/moment time instance\n * @returns {bool}\n */\n\n\nfunction areSameDay(a, b) {\n  const aTime = a.clone().startOf('day');\n  const bTime = b.clone().startOf('day');\n  return aTime.isSame(bTime);\n}\n/**\n * Determines whether the date is the start of a year, month or day.\n * @private\n * @param {object} date - @splunk/moment time instance\n * @returns {'year'|'month'|'date'|undefined}\n */\n\n\nfunction largestDateUnit(date) {\n  return (0, _lodash.find)(['year', 'month', 'date'], unit => {\n    const dateStart = date.clone().startOf(unit);\n    return date.isSame(dateStart);\n  });\n}\n/**\n * Finds the largest date unit that can describe a date range: years, months or days.\n * @private\n * @param {object} earliest - @splunk/moment time instance\n * @param {object} latest - @splunk/moment time instance\n * @returns {'year'|'month'|'date'|null}\n */\n\n\nfunction findSingleDateUnit(a, b) {\n  if (!(isWholeDay(a) && isWholeDay(b))) {\n    return null;\n  }\n\n  if (a.month() === 0 && b.month() === 0 && a.date() === 1 && b.date() === 1) {\n    return 'year';\n  }\n\n  if (a.year() === b.year() && a.date() === 1 && b.date() === 1 || a.year() + 1 === b.year() && b.month() === 0 && a.date() === 1 && b.date() === 1) {\n    return 'month';\n  }\n\n  return 'date';\n}\n/**\n * Generates a date range label for a single year, month or day.\n * For example:  '2017', 'Feb 2017' or 'Feb 18, 2017'\n * @private\n * @param {object} date - @splunk/moment time instance\n * @param {'year'|'month'|'date'} unit\n * @returns {string}\n */\n\n\nfunction createSingleUnitOnLabel(date, unit) {\n  switch (unit) {\n    case 'year':\n      return date.format('YYYY');\n\n    case 'month':\n      return (0, _format.sprintf)((0, _i18n._)('%(month)s %(year)s'), {\n        month: date.format('MMM'),\n        year: date.format('YYYY')\n      });\n\n    default:\n      return date.format('ll');\n  }\n}\n/**\n * Generates a date range label for a range of years, months or days.\n * For example:  '2015 though 2017', 'Feb – Apr, 2017' or 'Feb 17 – 18, 2017'\n * @private\n * @param {object} earliest - @splunk/moment time instance\n * @param {object} latest - @splunk/moment time instance\n * @param {'year'|'month'|'date'} unit\n * @returns {string}\n */\n\n\nfunction createSingleUnitThroughLabel(a, b, unit) {\n  const b2 = b.subtract(1, unit === 'date' ? 'day' : unit);\n\n  switch (unit) {\n    case 'year':\n      return (0, _format.sprintf)((0, _i18n._)('%(earliestYear)s – %(latestYear)s'), {\n        earliestYear: a.format('YYYY'),\n        latestYear: b2.format('YYYY')\n      });\n\n    case 'month':\n      return (0, _format.sprintf)((0, _i18n._)('%(earliestMonth)s – %(latestMonth)s, %(inYear)s'), {\n        earliestMonth: a.format('MMM'),\n        latestMonth: b2.format('MMM'),\n        inYear: b2.format('YYYY')\n      });\n\n    case 'date':\n      if (a.year() === b2.year()) {\n        if (a.month() === b2.month()) {\n          return (0, _format.sprintf)((0, _i18n._)('%(earliestMonth)s %(earliestDayOfMonth)s – %(latestDayOfMonth)s, %(latestYear)s'), {\n            earliestMonth: a.format('MMM'),\n            earliestDayOfMonth: a.format('D'),\n            latestDayOfMonth: b2.format('D'),\n            latestYear: b2.format('YYYY')\n          });\n        }\n\n        return (0, _format.sprintf)((0, _i18n._)('%(earliestMonth)s %(earliestDayOfMonth)s – %(latestMonth)s %(latestDayOfMonth)s, %(latestYear)s'), {\n          earliestMonth: a.format('MMM'),\n          earliestDayOfMonth: a.format('D'),\n          latestMonth: b2.format('MMM'),\n          latestDayOfMonth: b2.format('D'),\n          latestYear: b2.format('YYYY')\n        });\n      }\n\n      return (0, _format.sprintf)((0, _i18n._)('%(earliestMonth)s %(earliestDayOfMonth)s, %(earliestYear)s – %(latestMonth)s %(latestDayOfMonth)s, %(latestYear)s'), {\n        earliestMonth: a.format('MMM'),\n        earliestDayOfMonth: a.format('D'),\n        earliestYear: a.format('YYYY'),\n        latestMonth: b2.format('MMM'),\n        latestDayOfMonth: b2.format('D'),\n        latestYear: b2.format('YYYY')\n      });\n\n    default:\n      return (0, _format.sprintf)((0, _i18n._)('%(month)s %(earliestDayOfMonth)s – %(latestDayOfMonth)s, %(inYear)s'), {\n        month: a.format('MMM'),\n        earliestDayOfMonth: a.format('D'),\n        latestDayOfMonth: b2.format('D'),\n        inYear: b2.format('YYYY')\n      });\n  }\n}\n/**\n * Generates date-time label with the minimum specificity for time:minute, seconds or milliseconds.\n * For example:  'Feb 18, 2017 4:12:30.567 AM'\n * @private\n * @param {object} date - @splunk/moment time instance\n * @returns {string}\n */\n\n\nfunction createDateTimeLabel(date) {\n  try {\n    if (date.millisecond()) {\n      return date.splunkFormat('llms');\n    }\n\n    if (date.second()) {\n      return date.splunkFormat('lls');\n    }\n\n    return date.splunkFormat('lll');\n  } catch (e) {\n    return date.format('MMM D, YYYY H:mm.SS');\n  }\n}\n/**\n * Generates time label with the minimum specificity for time: minute, seconds or milliseconds.\n * For example:  '6:00 AM', '6:00:20 AM' or '6:00:20.712 AM'\n * @private\n * @param {object} time - @splunk/moment time instance\n * @returns {string}\n */\n\n\nfunction createTimeLabel(time) {\n  if (time.millisecond()) {\n    try {\n      return time.splunkFormat('LTMS');\n    } catch (e) {\n      return time.format('LTS');\n    }\n  }\n\n  if (time.second()) {\n    return time.format('LTS');\n  }\n\n  return time.format('LT');\n}\n/**\n * Generates a date-time label with the minimum specificity for time: minute, seconds or milliseconds.\n * For example: 'Feb 17, 2017 6:00 AM – Feb 18, 2017 12:20 AM'\n * @private\n * @param {object} earliestTime - @splunk/moment time instance\n * @param {object} latestTime - @splunk/moment time instance\n * @returns {string}\n */\n\n\nfunction createTimeRangeLabel(a, b) {\n  let format;\n\n  if (a.millisecond() || b.millisecond()) {\n    format = 'llms';\n  } else if (a.second() || b.second()) {\n    format = 'lls';\n  } else {\n    format = 'lll';\n  }\n\n  let aFormatted;\n  let bFormatted;\n\n  try {\n    aFormatted = a.splunkFormat(format);\n    bFormatted = b.splunkFormat(format);\n  } catch (e) {\n    aFormatted = a.format('lll');\n    bFormatted = b.format('lll');\n  }\n\n  return (0, _format.sprintf)((0, _i18n._)('%(earliestDateTime)s – %(latestDateTime)s'), {\n    earliestDateTime: aFormatted,\n    latestDateTime: bFormatted\n  });\n}\n/**\n * Generates a label for a time range on a single date.\n * For example: '6:00:20.850 AM to 8:00:40.490 PM, Feb 17, 2017'\n * @private\n * @param {object} earliestTime - @splunk/moment time instance\n * @param {object} latestTime - @splunk/moment time instance\n * @returns {string}\n */\n\n\nfunction createPartOfDayLabel(a, b) {\n  return (0, _format.sprintf)((0, _i18n._)('%(earliestTime)s – %(latestTime)s, %(date)s'), {\n    earliestTime: createTimeLabel(a),\n    latestTime: createTimeLabel(b),\n    date: a.format('ll')\n  });\n}\n\nfunction conformToMaxChars(label, shortLabel, maxChars) {\n  // if label is shorter than the shortLabel and longer than maxChars, use the label.\n  // This scenario may arise due to long translations of the shortLabel.\n  return maxChars && label.length > Math.max(shortLabel.length, maxChars) ? shortLabel : label;\n}\n/**\n * Generates a label for a date range when provided two ISO date formats.\n * @private\n * @param {string} earliest - An ISO time string, or other splunk time string.\n * @param {string} latest - An ISO time string, or other splunk time string .\n * @param {number} maxChars - If the generated label is too long, it will abbreviate\n * to a more generic form, such as 'Between Date-times' instead of 'Feb 17, 2017 6:00 AM to Feb 18, 2017 12:20 AM'.\n * @param {object|undefined} [momentTimeZone] - The unpacked moment timezone object that should be used for label creation/conversion.\n *  * Ex:\n * ```\n * {\n    name: '452846152',\n    abbrs: ['SST', 'SST'],\n    untils: [-1861879032000, null],\n    offsets: [660, 660]\n * }\n * @param {string} [momentTimeZone.name] - The string identifier of the timezone object.\n * @returns {null|string}\n */\n\n\nfunction createDateTimeRangeLabel(earliest, latest, maxChars, momentTimeZone) {\n  try {\n    // Load user timezone data and update default timezone for label formatting.\n    if (momentTimeZone != null && momentTimeZone.name) {\n      _moment.default.tz.add(_moment.default.tz.pack(momentTimeZone));\n\n      _moment.default.tz.setDefault(momentTimeZone.name);\n    }\n\n    const a = isISO(earliest) ? (0, _moment.default)(earliest).locale(_config.locale || 'en_US') : undefined;\n    const b = isISO(latest) ? (0, _moment.default)(latest).locale(_config.locale || 'en_US') : undefined;\n\n    if (a && b) {\n      // During Year, Month Day\n      const unit = findSingleDateUnit(a, b); // Single Year, Month or Day\n\n      const next = unit && (0, _moment.default)(a).add(1, unit === 'date' ? 'days' : unit);\n\n      if (unit) {\n        // Check if yesterday of date 'b' is the same as date 'a'\n        if (unit === 'date' && (0, _moment.default)(a).add(1, 'days').isSame(b)) {\n          return createSingleUnitOnLabel(a, unit);\n        } // For a single date unit of 'month' or 'year', check if date 'b' is the next 'month' or 'year', respectively\n\n\n        if (unit !== 'date' && next[unit]() === b[unit]()) {\n          return createSingleUnitOnLabel(a, unit);\n        }\n      }\n\n      if (unit) {\n        return conformToMaxChars(createSingleUnitThroughLabel(a, b, unit), (0, _i18n._)('Date Range'), maxChars);\n      } // Part of Day\n\n\n      if (areSameDay(a, b)) {\n        return conformToMaxChars(createPartOfDayLabel(a, b), (0, _i18n._)('Part of a Day'), maxChars);\n      } // Full\n\n\n      return conformToMaxChars(createTimeRangeLabel(a, b), (0, _i18n._)('Between Date-times'), maxChars);\n    }\n\n    if (a && isLatestNow(latest)) {\n      // Since\n      const unit = largestDateUnit(a);\n      const since = unit ? createSingleUnitOnLabel(a, unit) : createDateTimeLabel(a);\n      const longLabel = (0, _format.sprintf)((0, _i18n._)('Since %(dateTime)s'), {\n        dateTime: since\n      });\n      return conformToMaxChars(longLabel, (0, _i18n._)('Since Date-time'), maxChars);\n    }\n\n    if (isEarliestEmpty(earliest) && b) {\n      // Before\n      const unit = largestDateUnit(b);\n      const before = unit ? createSingleUnitOnLabel(b, unit) : createDateTimeLabel(b);\n      const longLabel = (0, _format.sprintf)((0, _i18n._)('Before %(dateTime)s'), {\n        dateTime: before\n      });\n      return conformToMaxChars(longLabel, (0, _i18n._)('Before Date-time'), maxChars);\n    }\n\n    return null;\n  } finally {\n    // Reset Default Timezone\n    _moment.default.tz.setDefault();\n  }\n}\n/**\n * Returns 'All-time' is the search is all-time or a close equivalent.\n * @private\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @returns {string}\n */\n\n\nfunction createAllTimeLabel(earliest, latest) {\n  if (isEarliestEmpty(earliest) && isLatestNow(latest)) {\n    return (0, _i18n._)('All time');\n  }\n\n  return false;\n}\n/**\n * Creates an appropriate label for a time range, using a preset label if available.\n * @public\n * @param {string} earliest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {string} latest - The time string such as `rt` , `rtnow` or `-2h@m`.\n * @param {object} [options] - An array of presents such as:\n * @param {string} [options.locale='en_US'] - An array of presents such as:\n * @param {number} [options.maxChars] - If the generated label is too long, it will abbreviate\n * to a more generic form, such as 'Between Date-times' instead of 'Feb 17, 2017 6:00 AM to Feb 18, 2017 12:20 AM'.\n * `Infinity` and `0` allow labels of any length.\n * @param {array} [options.presets] - An array of presents such as:\n * ```\n * [\n *     { label: '30 second window', earliest: 'rt-30s', latest: 'rt' },\n *     { label: 'Today', earliest: '@d', latest: 'now' },\n *     { label: 'Previous year', earliest: '-1y@y', latest: '@y' },\n *     { label: 'Last 15 minutes', earliest: '-15m', latest: 'now' },\n *     { label: 'All time', earliest: '0', latest: '' },\n * ]\n * ```\n * @param {object} [options.momentTimeZone] - The unpacked moment timezone object that should be used for label creation/conversion. \n * Ex:\n * ```\n * {\n    name: '452846152',\n    abbrs: ['SST', 'SST'],\n    untils: [-1861879032000, null],\n    offsets: [660, 660]\n * }\n * ```\n * @returns {string} Returns `'Custom time'` if one cannot be made.\n */\n\n\nfunction createRangeLabel(earliest, latest, {\n  presets,\n  maxChars,\n  momentTimeZone\n} = {}) {\n  const earliestTrimed = earliest.trim();\n  const latestTrimed = latest.trim();\n  return createAllTimeLabel(earliestTrimed, latestTrimed) || presets && findPresetLabel(presets, earliestTrimed, latestTrimed) || createRealTimeLabel(earliestTrimed, latestTrimed) || createRelativeTimeLabel(earliestTrimed, latestTrimed) || createDateTimeRangeLabel(earliestTrimed, latestTrimed, maxChars, momentTimeZone) || (0, _i18n._)('Custom time');\n}\n\nfunction pluralize(singular, plural, numberOf) {\n  if (numberOf === 0) {\n    return null;\n  }\n\n  return numberOf === 1 ? singular : (0, _format.sprintf)(plural, {\n    numberOf\n  });\n}\n/**\n * Takes a duration in milliseconds and returns a string describing the duration in terms of\n * years, months, days, hours, minutes, seconds and milliseconds.\n * If a unit isn't needed it's omitted, e.g. durations less than a year won't include '0 years'.\n * @public\n * @param {string|number} ms - The duration in milliseconds.\n * @returns {string} A formatted duration string, for example `27 days 16 hours 36 minutes 59 seconds`.\n * Durations <= 0 return `null`.\n */\n\n\nfunction formatDuration(ms) {\n  if (ms <= 0) {\n    return null;\n  }\n\n  const duration = _moment.default.duration(ms);\n\n  return [pluralize((0, _i18n._)('1 year'), (0, _i18n._)('%(numberOf)d years'), duration.years()), pluralize((0, _i18n._)('1 month'), (0, _i18n._)('%(numberOf)d months'), duration.months()), pluralize((0, _i18n._)('1 day'), (0, _i18n._)('%(numberOf)d days'), duration.days()), pluralize((0, _i18n._)('1 hour'), (0, _i18n._)('%(numberOf)d hours'), duration.hours()), pluralize((0, _i18n._)('1 minute'), (0, _i18n._)('%(numberOf)d minutes'), duration.minutes()), pluralize((0, _i18n._)('1 second'), (0, _i18n._)('%(numberOf)d seconds'), duration.seconds()), pluralize((0, _i18n._)('1 millisecond'), (0, _i18n._)('%(numberOf)d milliseconds'), duration.milliseconds())].filter(display => !!display).join(' ');\n}\n/**\n * Gets the number of units from the earliest string\n * @param {string} earliest\n * @returns {number}\n */\n\n\nconst getFromNumber = earliest => {\n  const parseEarliest = parseTimeString(earliest).modifiers[0];\n  return parseEarliest && Math.abs(parseInt(parseEarliest.amount, 10)) || 0;\n};\n/**\n * Returns true if the earliest string contains a snap unit\n * @param {string} earliest\n * @returns {boolean}\n */\n\n\nexports.getFromNumber = getFromNumber;\n\nconst getFromSnap = earliest => {\n  const parseEarliest = parseTimeString(earliest).modifiers[0];\n  return !!parseEarliest && !!parseEarliest.snap;\n};\n/**\n * Returns the time unit of the earliest string\n * Defaults to seconds\n * @param {string} earliest\n * @returns {string}\n */\n\n\nexports.getFromSnap = getFromSnap;\n\nconst getFromUnit = earliest => {\n  const parseEarliest = parseTimeString(earliest).modifiers[0];\n  return parseEarliest && parseEarliest.unit || 's';\n};\n/**\n * Returns if the latest string has a snap unit\n * @param {string} latest\n */\n\n\nexports.getFromUnit = getFromUnit;\n\nconst getToSnap = latest => {\n  const parseLatest = parseTimeString(latest).modifiers[0];\n  return parseLatest && !!parseLatest.snap || false;\n};\n/**\n * Returns the earliest time string in relative format\n * @param {number} fromNumber\n * @param {boolean} fromSnap\n * @param {string} fromUnit\n * @returns {string}\n */\n\n\nexports.getToSnap = getToSnap;\n\nconst getRelativeEarliest = (fromNumber, fromSnap, fromUnit) => {\n  const earliestSnap = fromSnap ? \"@\" + fromUnit : '';\n  return \"-\" + fromNumber + fromUnit + earliestSnap;\n};\n/**\n * Returns the latest time string in relative format\n * @param {string} fromUnit\n * @param {boolean} toSnap\n */\n\n\nexports.getRelativeEarliest = getRelativeEarliest;\n\nconst getRelativeLatest = (fromUnit, toSnap) => {\n  return toSnap ? \"@\" + fromUnit : 'now';\n};\n/**\n * Returns the earliest time string in real time format\n * @param {number} fromNumber\n * @param {string} fromUnit\n * @return {string}\n */\n\n\nexports.getRelativeLatest = getRelativeLatest;\n\nconst getRealTimeEarliest = (fromNumber, fromUnit) => {\n  return \"rt-\" + fromNumber + fromUnit;\n};\n/**\n * Returns the earliest and latest time range based on duration\n * @param {time} string (ISO format)\n * @param {number} duration\n * @param {string} unit\n * @return {earliest, latest}\n */\n\n\nexports.getRealTimeEarliest = getRealTimeEarliest;\n\nconst getAcceleratedTimeRange = (time, duration, unit) => {\n  const format = 'YYYY-MM-DDTHH:mm:ss.SSSZ';\n  const earliest = getISOWithTimeZone((0, _moment.default)(time).subtract(duration, unit).format(format));\n  const latest = getISOWithTimeZone((0, _moment.default)(time).add(duration, unit).format(format));\n  return {\n    earliest,\n    latest\n  };\n};\n\nexports.getAcceleratedTimeRange = getAcceleratedTimeRange;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/time.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/timeParser.js":
/*!*************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/timeParser.js ***!
  \*************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nvar _interopRequireDefault = __webpack_require__(/*! @babel/runtime/helpers/interopRequireDefault */ \"./node_modules/@babel/runtime/helpers/interopRequireDefault.js\");\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.getISO = getISO;\nexports[\"default\"] = void 0;\n\nvar _querystring = _interopRequireDefault(__webpack_require__(/*! querystring */ \"./node_modules/querystring/index.js\"));\n\nvar _lodash = __webpack_require__(/*! lodash */ \"./node_modules/lodash/lodash.js\");\n\nvar _url = __webpack_require__(/*! @splunk/splunk-utils/url */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/url.js\");\n\nvar _fetch = __webpack_require__(/*! @splunk/splunk-utils/fetch */ \"./node_modules/@splunk/time-range-utils/node_modules/@splunk/splunk-utils/fetch.js\");\n\n/**\n * This utility currently only has one function for parsing a time string on the server.\n * ```\n *  import { getISO } from '@splunk/time-range-utils/timeParser'\n * ```\n *\n * @file\n */\n\n/**\n * Fetches the parsed time from splunkd with a browser fetch.\n * When the promise is resolved, a data object is returned with up to three properties:\n * * time (the original time provided for for validation and identification purposes)\n * * iso (the parsed time value)\n * * error.\n *\n * Example usage:\n * ```\n * getISO('-2d@h')\n *     .then(data => {\n *         this.setState({\n *             parseLatest: {error: data.error, iso: data.iso, time: data.time, },\n *         });\n *     })\n *     .catch(data => {\n *         this.setState({\n *             parseLatest: {error: data.error, iso: '', time: data.time, },\n *         });\n *     });\n * ```\n * @public\n * @function\n * @name getISO\n * @param {string} time - The time string such as `47165491` or `-2h@m`.\n * @returns {promise}\n */\nfunction getISO(time) {\n  const qs = _querystring.default.stringify({\n    output_mode: 'json',\n    time\n  });\n\n  const path = (0, _url.createRESTURL)('search/timeparser');\n  return fetch(path + \"?\" + qs, (0, _fetch.getDefaultFetchInit)()).then((0, _fetch.handleResponse)(200)).then(data => {\n    // can get away with doing this below because only one timestamp will be returned\n    const value = (0, _lodash.keys)(data)[0];\n    return {\n      error: null,\n      iso: data[value],\n      time\n    };\n  }).catch(error => ({\n    error,\n    time\n  }));\n}\n\nvar _default = {\n  getISO\n};\nexports[\"default\"] = _default;\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/timeParser.js?");

/***/ }),

/***/ "./node_modules/@splunk/time-range-utils/timeUnits.js":
/*!************************************************************!*\
  !*** ./node_modules/@splunk/time-range-utils/timeUnits.js ***!
  \************************************************************/
/***/ (function(__unused_webpack_module, exports, __webpack_require__) {

eval("\n\nvar _interopRequireDefault = __webpack_require__(/*! @babel/runtime/helpers/interopRequireDefault */ \"./node_modules/@babel/runtime/helpers/interopRequireDefault.js\");\n\nObject.defineProperty(exports, \"__esModule\", ({\n  value: true\n}));\nexports.snapUnits = exports.timeUnits = void 0;\n\nvar _defineProperty2 = _interopRequireDefault(__webpack_require__(/*! @babel/runtime/helpers/defineProperty */ \"./node_modules/@babel/runtime/helpers/defineProperty.js\"));\n\nvar _i18n = __webpack_require__(/*! @splunk/ui-utils/i18n */ \"./node_modules/@splunk/ui-utils/i18n.js\");\n\nfunction ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }\n\nfunction _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { (0, _defineProperty2.default)(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }\n\nconst u = {};\n/* eslint-disable no-multi-assign */\n\nu.s = u.sec = u.secs = u.second = u.seconds = {\n  abbr: 's',\n  singular: (0, _i18n._)('second'),\n  plural: (0, _i18n._)('seconds')\n};\nu.m = u.min = u.mins = u.minute = u.minutes = {\n  abbr: 'm',\n  singular: (0, _i18n._)('minute'),\n  plural: (0, _i18n._)('minutes')\n};\nu.h = u.hr = u.hrs = u.hour = u.hours = {\n  abbr: 'h',\n  singular: (0, _i18n._)('hour'),\n  plural: (0, _i18n._)('hours')\n};\nu.d = u.day = u.days = {\n  abbr: 'd',\n  singular: (0, _i18n._)('day'),\n  plural: (0, _i18n._)('days')\n};\nu.w = u.week = u.weeks = {\n  abbr: 'w',\n  singular: (0, _i18n._)('week'),\n  plural: (0, _i18n._)('weeks')\n};\nu.mon = u.month = u.months = {\n  abbr: 'mon',\n  singular: (0, _i18n._)('month'),\n  plural: (0, _i18n._)('months')\n};\nu.q = u.qtr = u.qtrs = u.quarter = u.quarters = {\n  abbr: 'q',\n  singular: (0, _i18n._)('quarter'),\n  plural: (0, _i18n._)('quarters')\n};\nu.y = u.yr = u.yrs = u.year = u.years = {\n  abbr: 'y',\n  singular: (0, _i18n._)('year'),\n  plural: (0, _i18n._)('years')\n};\n/* eslint-enable no-multi-assign */\n\nconst timeUnits = u; // snap units can additionally include days of the week.\n\nexports.timeUnits = timeUnits;\n\nconst snapUnits = _objectSpread({}, u);\n\nexports.snapUnits = snapUnits;\nsnapUnits.w0 = {\n  abbr: 'w0',\n  singular: (0, _i18n._)('Sunday'),\n  plural: (0, _i18n._)('Sundays')\n};\nsnapUnits.w1 = {\n  abbr: 'w1',\n  singular: (0, _i18n._)('Monday'),\n  plural: (0, _i18n._)('Mondays')\n};\nsnapUnits.w2 = {\n  abbr: 'w2',\n  singular: (0, _i18n._)('Tuesday'),\n  plural: (0, _i18n._)('Tuesdays')\n};\nsnapUnits.w3 = {\n  abbr: 'w3',\n  singular: (0, _i18n._)('Wednesday'),\n  plural: (0, _i18n._)('Wednesdays')\n};\nsnapUnits.w4 = {\n  abbr: 'w4',\n  singular: (0, _i18n._)('Thursday'),\n  plural: (0, _i18n._)('Thursdays')\n};\nsnapUnits.w5 = {\n  abbr: 'w5',\n  singular: (0, _i18n._)('Friday'),\n  plural: (0, _i18n._)('Fridays')\n};\nsnapUnits.w6 = {\n  abbr: 'w6',\n  singular: (0, _i18n._)('Saturday'),\n  plural: (0, _i18n._)('Saturdays')\n};\nsnapUnits.w7 = {\n  abbr: 'w7',\n  singular: (0, _i18n._)('Sunday'),\n  plural: (0, _i18n._)('Sundays')\n};\n\n//# sourceURL=webpack://client/./node_modules/@splunk/time-range-utils/timeUnits.js?");

/***/ })

}]);